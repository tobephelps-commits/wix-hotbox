---
phase: 59-v1x-feature-parity-audit
plan: 05
type: execute
wave: 2
depends_on: ["59-01", "59-02", "59-03", "59-04"]
files_modified:
  - ui/src/components/orders/OrderDetail.tsx
  - ui/src/components/orders/OrderDetail.css
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
---

<objective>
Integration polish and build validation for all Phase 59 features.

Purpose: Verify all 4 new features integrate cleanly, fix any CSS inconsistencies with design tokens, ensure production build passes, and update project state.
Output: Clean production build, consistent styling across all new components, updated STATE.md and ROADMAP.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/59-v1x-feature-parity-audit/59-01-SUMMARY.md
@.planning/phases/59-v1x-feature-parity-audit/59-02-SUMMARY.md
@.planning/phases/59-v1x-feature-parity-audit/59-03-SUMMARY.md
@.planning/phases/59-v1x-feature-parity-audit/59-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-feature CSS audit and integration fixes</name>
  <files>ui/src/components/orders/OrderDetail.tsx, ui/src/components/orders/OrderDetail.css</files>
  <action>
1. Read all new CSS files created in plans 01-04 (OrderCreateForm.css, CartFillModal.css, BatchCreateForm.css)
2. Verify ALL use design tokens from Phase 58 (--radius-md, --spacing-*, --font-size-*, --shadow-depth-*, --color-*)
3. Check for any hardcoded values (colors, border-radius, font-sizes, shadows) and replace with tokens
4. Verify touch targets are 56px+ on all interactive elements
5. Verify :focus-visible styles exist on new interactive elements (following Phase 58-06 pattern)
6. Check OrderDetail Documents section — ensure Label, Production Sheet, and Invoice buttons are visually balanced
7. Fix any CSS specificity or ordering issues between new and existing styles
8. Ensure all modals (OrderCreateForm, CartFillModal) have consistent overlay/backdrop styling
  </action>
  <verify>npm run build from ui/ directory succeeds with no warnings</verify>
  <done>All new CSS uses design tokens, no hardcoded values, consistent modal/button styling, touch targets 56px+</done>
</task>

<task type="auto">
  <name>Task 2: Production build validation and state update</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
1. Run full production build: `npm run build` from project root (compiles TypeScript backend)
2. Run frontend build: `cd ui && npm run build` (Vite production build)
3. If any build errors, fix them
4. Update STATE.md:
   - Current Position: Phase 59 complete
   - Last activity date and description
   - Add Phase 59 decisions table
   - Update session continuity
5. Update ROADMAP.md:
   - Mark Phase 59 as complete with plan count
   - Update progress table
  </action>
  <verify>Both npm run build (root) and npm run build (ui/) succeed with zero errors</verify>
  <done>Clean production builds, STATE.md and ROADMAP.md reflect Phase 59 completion</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Backend TypeScript build passes
- [ ] Frontend Vite production build passes
- [ ] No hardcoded CSS values in new components
- [ ] All touch targets 56px+
- [ ] STATE.md updated with Phase 59 complete
- [ ] ROADMAP.md progress table updated
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Clean production builds
- Project state accurately reflects Phase 59 completion
</success_criteria>

<output>
After completion, create `.planning/phases/59-v1x-feature-parity-audit/59-05-SUMMARY.md`
</output>
