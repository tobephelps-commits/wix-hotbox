---
phase: 58-kiosk-touch-ui-modernization
plan: 06
type: execute
wave: 3
depends_on: ["58-02", "58-03", "58-04", "58-05"]
files_modified: [ui/src/index.css, ui/src/App.css, ui/src/App.tsx]
autonomous: true
---

<objective>
Final integration polish — verify design token consistency across all tabs, fix any visual inconsistencies, ensure responsive breakpoints work with new sizing, and do a build validation.

Purpose: After parallel modernization of all tabs, ensure everything works together — no conflicting styles, no broken layouts, no missing token references.
Output: Clean build, any fixups applied, all responsive breakpoints verified in CSS.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-kiosk-touch-ui-modernization/58-01-SUMMARY.md
@.planning/phases/58-kiosk-touch-ui-modernization/58-02-SUMMARY.md
@.planning/phases/58-kiosk-touch-ui-modernization/58-03-SUMMARY.md
@.planning/phases/58-kiosk-touch-ui-modernization/58-04-SUMMARY.md
@.planning/phases/58-kiosk-touch-ui-modernization/58-05-SUMMARY.md

@ui/src/index.css
@ui/src/App.css
@ui/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix cross-tab consistency</name>
  <files>ui/src/index.css, ui/src/App.css</files>
  <action>
After all parallel plans have completed, do a consistency audit:

1. **Grep for hardcoded values that should be tokens:**
   - Search all .css files in ui/src/ for `border-radius: [0-9]+px` that should use --radius-* tokens
   - Search for `min-height: 44px` that should be var(--touch-min)
   - Search for `font-size: 0\.[0-9]` that should use --font-* tokens
   - Search for `transition:.*0\.15s` that should use var(--transition-base)

   Fix any remaining hardcoded values found.

2. **Check responsive breakpoints work with new sizing:**
   - 768px breakpoint: sidebar at 56px (was 48px), ensure content doesn't overflow
   - 480px breakpoint: bottom nav updated for new tab sizing, content padding-bottom adequate
   - 900px breakpoint: orders layout single-column triggers correctly

3. **Add any missing global refinements to index.css:**
   - If not already done, add focus-visible styles for keyboard navigation:
     ```css
     :focus-visible {
       outline: 2px solid var(--color-accent);
       outline-offset: 2px;
     }
     ```
   - Ensure all `select` elements have appearance styled consistently

4. **Verify App.tsx has no hardcoded inline styles** that conflict with CSS changes. If header has inline styles, move to CSS.
  </action>
  <verify>Build succeeds: cd ui && npx vite build --mode development 2>&1 | tail -5</verify>
  <done>No hardcoded values remain where tokens should be used, responsive breakpoints verified, focus styles added</done>
</task>

<task type="auto">
  <name>Task 2: Full production build validation</name>
  <files>ui/src/App.tsx</files>
  <action>
Run a full production build to validate everything compiles cleanly:

1. Run `cd ui && npx vite build` (production build, not dev mode)
2. Check build output for warnings or errors
3. Verify output bundle exists in ui/dist/
4. Check that all CSS is properly bundled (no missing @import references)

If any TypeScript errors are found in the build, fix them. The changes in this phase are CSS-only, so TSX errors would indicate a regression from Plan 02's Sidebar.tsx changes.

No structural changes to App.tsx unless fixing build errors. The goal is pure validation.
  </action>
  <verify>cd ui && npx vite build 2>&1 | tail -10 shows successful build with no errors</verify>
  <done>Production build succeeds cleanly, all CSS bundled correctly, no warnings</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd ui && npx vite build` succeeds (production mode)
- [ ] No hardcoded 44px touch targets remain in CSS
- [ ] Consistent border-radius tokens across all files
- [ ] Focus-visible styles added for accessibility
- [ ] All responsive breakpoints account for new larger sizing
</verification>

<success_criteria>

- All tasks completed
- Clean production build
- Design token usage is consistent across all CSS files
- Responsive breakpoints adjusted for larger element sizes
- No regressions introduced
</success_criteria>

<output>
After completion, create `.planning/phases/58-kiosk-touch-ui-modernization/58-06-SUMMARY.md`
</output>
