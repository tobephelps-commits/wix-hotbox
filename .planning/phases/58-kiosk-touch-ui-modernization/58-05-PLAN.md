---
phase: 58-kiosk-touch-ui-modernization
plan: 05
type: execute
wave: 2
depends_on: ["58-01"]
files_modified: [ui/src/components/inventory/InventoryTab.css, ui/src/components/customers/CustomersTab.css, ui/src/components/system/SystemTab.css, ui/src/components/logos/LogoManager.css]
autonomous: true
---

<objective>
Modernize Inventory, Customers, System, and Logo Manager tab UIs for touchscreen kiosk — consistent rounded cards, larger text, bigger touch targets across all remaining tabs.

Purpose: All tabs must feel unified in the modernized design. These four smaller tabs share similar patterns (cards, lists, buttons) and can be updated together.
Output: Updated CSS files for all four remaining tab components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-kiosk-touch-ui-modernization/58-01-SUMMARY.md

@ui/src/components/inventory/InventoryTab.css
@ui/src/components/customers/CustomersTab.css
@ui/src/components/system/SystemTab.css
@ui/src/components/logos/LogoManager.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modernize Inventory and Customers tab styles</name>
  <files>ui/src/components/inventory/InventoryTab.css, ui/src/components/customers/CustomersTab.css</files>
  <action>
Read both CSS files first to understand current styles. Then apply the design token pattern consistently:

**For both tabs, apply these rules to all matching selectors:**

1. **Cards/panels:** border-radius: var(--radius-lg), box-shadow: 0 2px 8px var(--color-shadow), padding increase by ~20%
2. **Buttons:** min-height: var(--touch-min) (56px), border-radius: var(--radius-md) or var(--radius-pill) for pill buttons, font-size: var(--font-sm) or var(--font-base)
3. **Inputs:** height: var(--touch-min), border-radius: var(--radius-md), font-size: var(--font-base), focus: box-shadow: 0 0 0 3px var(--color-accent-glow)
4. **Section titles/labels:** font-size: var(--font-sm) for small labels, var(--font-base) for regular, var(--font-xl) for page titles
5. **Table rows/list items:** increase min-height, larger fonts, more padding
6. **Badges/chips:** border-radius: var(--radius-pill), padding: 4px 12px, font-size: var(--font-xs)
7. **Transitions:** all transition durations use var(--transition-base)
8. **Empty states:** larger text, more padding

**Inventory-specific:**
- Alert cards: larger padding, rounded corners, bigger text for alert messages
- Tracked product rows: taller for easier touch selection
- Polling status indicators: bigger icons/text
- Snapshot data cards: rounded, shadowed

**Customers-specific:**
- Customer list rows: taller, larger name/email text
- Customer detail cards: rounded, shadowed
- Pricing configuration inputs: match Products tab sizing
- Royalty report buttons: 56px+ height
  </action>
  <verify>Build succeeds: cd ui && npx vite build --mode development 2>&1 | tail -5</verify>
  <done>Inventory and Customers tabs consistently use design tokens with 56px+ touch targets, rounded cards, larger text</done>
</task>

<task type="auto">
  <name>Task 2: Modernize System tab and Logo Manager styles</name>
  <files>ui/src/components/system/SystemTab.css, ui/src/components/logos/LogoManager.css</files>
  <action>
Read both CSS files first. Then apply design tokens:

**System Tab:**
- Health dashboard cards: border-radius: var(--radius-lg), box-shadow: 0 2px 8px var(--color-shadow), padding: 24px
- Metric values: font-size: var(--font-2xl) or larger for big numbers
- Metric labels: font-size: var(--font-sm)
- Status indicators: larger dots/icons
- Action buttons (Start/Stop daemon, etc.): min-height: var(--touch-min), border-radius: var(--radius-md), font-size: var(--font-base)
- Grid gaps: increase for more breathing room
- Printer cards: rounded, shadowed, larger text
- Section headers: font-size: var(--font-lg) or var(--font-xl)

**Logo Manager:**
- Logo grid cards: border-radius: var(--radius-lg), box-shadow, larger thumbnails
- Upload button: min-height: var(--touch-lg) (64px), prominent styling
- Logo name text: font-size: var(--font-base)
- Delete/action buttons: min-height: var(--touch-min), border-radius: var(--radius-md)
- Upload drop zone: rounded corners, larger text
- Logo detail view: larger preview, bigger controls

All transitions: var(--transition-base). All interactive elements: 56px+ touch targets.
  </action>
  <verify>Build succeeds: cd ui && npx vite build --mode development 2>&1 | tail -5</verify>
  <done>System tab and Logo Manager use modernized kiosk styling with design tokens</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd ui && npx vite build --mode development` succeeds
- [ ] All four tab CSS files updated with design tokens
- [ ] No hardcoded pixel values for touch targets (all use --touch-min or --touch-lg)
- [ ] Card border-radius consistent (--radius-lg) across all tabs
</verification>

<success_criteria>

- All tasks completed
- All four remaining tabs use consistent design token sizing
- Touch targets at 56px+ on all interactive elements
- Cards have rounded corners (16px) with soft shadows
- Typography uses token-based sizing throughout
</success_criteria>

<output>
After completion, create `.planning/phases/58-kiosk-touch-ui-modernization/58-05-SUMMARY.md`
</output>
