---
phase: 58-kiosk-touch-ui-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [ui/src/index.css, ui/src/App.css]
autonomous: true
---

<objective>
Establish modernized design system foundation with updated design tokens, typography scale, spacing system, and base component styles for 15.6" touchscreen kiosk.

Purpose: All subsequent plans depend on these tokens. Centralizing the design system ensures consistency across all tab modernizations running in parallel.
Output: Updated index.css and App.css with new design tokens, typography, base element styles, transition utilities, and layout adjustments.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@ui/src/index.css
@ui/src/App.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update design tokens and base styles in index.css</name>
  <files>ui/src/index.css</files>
  <action>
Update CSS custom properties in :root to add new design tokens for the modernized kiosk UI:

**New color tokens (keep existing, add these):**
- --color-surface-hover: #222240
- --color-surface-active: #12122a
- --color-accent-hover: #5a52e0
- --color-accent-glow: rgba(108, 99, 255, 0.25)
- --color-shadow: rgba(0, 0, 0, 0.3)

**New spacing tokens:**
- --radius-sm: 8px
- --radius-md: 12px
- --radius-lg: 16px
- --radius-xl: 20px
- --radius-pill: 9999px

**New sizing tokens:**
- --touch-min: 56px (increased from 44px)
- --touch-lg: 64px

**Typography tokens:**
- --font-xs: 0.8rem (was 0.7rem range)
- --font-sm: 0.9rem (was 0.8rem range)
- --font-base: 1rem (was 0.85-0.9rem range)
- --font-lg: 1.15rem (was 1rem range)
- --font-xl: 1.35rem (was 1.25rem range)
- --font-2xl: 1.6rem (was 1.3rem range)
- --font-3xl: 2rem (new)

**Transition tokens:**
- --transition-fast: 120ms ease
- --transition-base: 200ms ease
- --transition-slow: 300ms ease

Update base element styles:
- Change html font-size from 16px to 17px for larger base text
- Update min-height on touchable elements from 44px to var(--touch-min) (56px)
- Widen scrollbar from 6px to 10px for touch-friendly scrolling
- Add scrollbar-gutter: stable to body for consistent layout
- Add touch ripple keyframe animation:
  ```
  @keyframes touch-ripple {
    0% { transform: scale(0.95); opacity: 0.7; }
    100% { transform: scale(1); opacity: 1; }
  }
  ```
- Add .press-feedback utility class that applies touch-ripple on :active
- Update .touchable:active to use transform: scale(0.97) instead of opacity change for better tactile feedback
  </action>
  <verify>Build succeeds: cd ui && npx vite build --mode development 2>&1 | tail -5</verify>
  <done>All design tokens defined in :root, base font size increased, touch targets at 56px, scrollbar widened, transition utilities defined, ripple animation added</done>
</task>

<task type="auto">
  <name>Task 2: Modernize App layout shell — header, sidebar area, content area</name>
  <files>ui/src/App.css</files>
  <action>
Update the app-level layout for the modernized design:

**Header (.app-header):**
- Increase height from 56px to 64px (grid-template-rows: 64px 1fr)
- Add padding: 0 24px (was 0 20px)
- Add subtle bottom shadow: box-shadow: 0 2px 8px var(--color-shadow)
- Remove border-bottom (shadow replaces it)
- Title: font-size: var(--font-xl), letter-spacing: 0.03em
- Status: font-size: var(--font-sm)

**Sidebar area (.app-sidebar):**
- Increase width from 200px to 220px (grid-template-columns: 220px 1fr)
- Add subtle right shadow instead of border: box-shadow: 2px 0 8px var(--color-shadow), remove border-right
- Padding: 12px 0 (was 16px 0)
- Section label (.app-sidebar__label): font-size: var(--font-xs), padding: 14px 24px

**Content area (.app-content):**
- Increase padding from 24px to 28px
- Add scroll-padding-top: 16px for better scroll snap behavior

**Responsive breakpoints:**
- Tablet (768px): sidebar 56px (was 48px), grid-template-rows: 64px 1fr
- Mobile (480px): header stays 64px, bottom nav height auto, content padding-bottom: 80px (was 72px)

All values should reference design tokens where applicable (--radius-md for any border-radius, --transition-base for transitions, etc.).
  </action>
  <verify>Build succeeds: cd ui && npx vite build --mode development 2>&1 | tail -5</verify>
  <done>App layout uses updated sizing — 64px header, 220px sidebar, shadow-based separation, token-referenced values throughout</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd ui && npx vite build --mode development` succeeds without errors
- [ ] All new CSS custom properties defined and syntactically valid
- [ ] No hardcoded values where tokens should be used in modified code
- [ ] Responsive breakpoints still functional (no broken grid at 768px and 480px)
</verification>

<success_criteria>

- All tasks completed
- Design tokens centralized in :root
- Base font size increased for better readability on 15.6" screen
- Touch targets increased to 56px minimum
- Shadow-based visual separation replaces hard borders on header/sidebar
- All verification checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/58-kiosk-touch-ui-modernization/58-01-SUMMARY.md`
</output>
