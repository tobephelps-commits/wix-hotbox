---
phase: 58-kiosk-touch-ui-modernization
plan: 02
type: execute
wave: 2
depends_on: ["58-01"]
files_modified: [ui/src/components/Sidebar.css, ui/src/components/Sidebar.tsx]
autonomous: true
---

<objective>
Modernize sidebar navigation for comfortable finger operation — larger icons, wider touch targets, rounded active highlights, smooth transitions.

Purpose: Primary navigation must be effortless on a touchscreen. Current 56px tabs with small icons need to be larger and more tactile.
Output: Updated Sidebar with 64px touch targets, 24px icons, rounded active state with accent background, press feedback animation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-kiosk-touch-ui-modernization/58-01-SUMMARY.md

@ui/src/components/Sidebar.css
@ui/src/components/Sidebar.tsx
@ui/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modernize sidebar tab styles</name>
  <files>ui/src/components/Sidebar.css</files>
  <action>
Update sidebar styles for kiosk touch optimization:

**Tab buttons (.sidebar-tab):**
- min-height: var(--touch-lg) (64px, was 56px)
- padding: 0 20px (was 0 16px)
- gap: 14px (was 12px)
- font-size: var(--font-base) (was 0.9rem)
- border-left: none (remove left border indicator)
- border-radius: 0 (will add rounded active state differently)
- transition: all var(--transition-base)

**Active state (.sidebar-tab--active):**
- Replace border-left-color with a rounded background pill approach:
  - margin: 0 10px
  - border-radius: var(--radius-md)
  - background: rgba(108, 99, 255, 0.15)
  - color: var(--color-accent)
  - box-shadow: inset 0 0 0 1px rgba(108, 99, 255, 0.2)
- Remove border-left-color: var(--color-accent)

**Hover state (.sidebar-tab:hover):**
- margin: 0 10px
- border-radius: var(--radius-md)
- background: var(--color-surface-hover)

**Active press (.sidebar-tab:active):**
- transform: scale(0.97)
- transition: transform 80ms ease

**Icons (.sidebar-tab__icon):**
- font-size: 1.5rem (was 1.25rem)
- width: 28px (was 20px)

**Labels (.sidebar-tab__label):**
- font-weight: 600 (was 500)
- font-size: var(--font-base)

**Gap between tabs (.sidebar__main):**
- gap: 4px (was 2px)
- padding: 12px 0 (was 8px 0)

**Separator (.sidebar__separator):**
- margin: 4px 20px 8px (was 0 16px 8px)
- border-radius: 1px

**Responsive tablet (768px):**
- .sidebar-tab: min-height: var(--touch-min) (56px), justify-content: center, padding: 0, margin: 0 4px, border-radius: var(--radius-sm)
- .sidebar-tab__icon: width: auto, font-size: 1.4rem

**Responsive mobile (480px):**
- .sidebar-tab: min-height: var(--touch-min), margin: 0, border-radius: 0
- Active state: border-radius: 0, margin: 0
- Replace border-top indicator with background highlight + bottom accent line:
  - .sidebar-tab--active: border-top: none, border-bottom: 3px solid var(--color-accent), background: rgba(108, 99, 255, 0.1)
- .sidebar-tab__label: font-size: 0.7rem (was 0.65rem)
  </action>
  <verify>Build succeeds: cd ui && npx vite build --mode development 2>&1 | tail -5</verify>
  <done>Sidebar tabs are 64px tall with rounded active highlights, 24px+ icons, press feedback, mobile bottom nav updated</done>
</task>

<task type="auto">
  <name>Task 2: Update Sidebar.tsx icon sizes and touch attributes</name>
  <files>ui/src/components/Sidebar.tsx</files>
  <action>
Minor TSX updates to support modernized sidebar:

1. If icons are rendered as emoji/text characters, no change needed. If they use a size prop or inline style, increase to match new 1.5rem icon size.

2. Add `role="navigation"` and `aria-label="Main navigation"` to the sidebar container element if not already present, for accessibility.

3. Ensure all tab buttons have `type="button"` attribute to prevent form submission behavior in any context.

4. No structural changes — the component logic (active tab, localStorage persistence, click handlers) remains identical.

Only modify what's necessary. If the TSX already has proper button types and accessibility attributes, make no changes to this file.
  </action>
  <verify>Build succeeds: cd ui && npx vite build --mode development 2>&1 | tail -5</verify>
  <done>Sidebar TSX has proper accessibility attributes and button types</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd ui && npx vite build --mode development` succeeds
- [ ] Sidebar tabs visually larger (64px min-height in CSS)
- [ ] Active state uses rounded pill background (not left border)
- [ ] Responsive breakpoints still work (tablet icon-only, mobile bottom nav)
</verification>

<success_criteria>

- All tasks completed
- Sidebar tabs are 64px with larger icons and labels
- Active state is rounded pill with accent background
- Press feedback animation applied
- Responsive layouts updated for new sizing
</success_criteria>

<output>
After completion, create `.planning/phases/58-kiosk-touch-ui-modernization/58-02-SUMMARY.md`
</output>
