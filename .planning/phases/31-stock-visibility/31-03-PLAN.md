---
phase: 31-stock-visibility
plan: 03
type: execute
wave: 3
depends_on: ["31-02"]
files_modified:
  - scripts/OPERATIONS.md
autonomous: false
---

<objective>
Verify stock visibility behavior end-to-end and update operational documentation.

Purpose: Confirm that out-of-stock variants now display "Out of Stock" in the WIX storefront instead of being hidden, and document the behavior change for operations.

Output: Verified stock visibility in live storefront, updated OPERATIONS.md with new behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plans
@.planning/phases/31-stock-visibility/31-01-SUMMARY.md
@.planning/phases/31-stock-visibility/31-02-SUMMARY.md

# Docs to update
@scripts/OPERATIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test stock sync with new inventory API</name>
  <files></files>
  <action>
Run the stock sync to verify it uses the new inventory API:

1. Start the sync daemon or run single sync:
```bash
npm run sync:run
```

2. Check console output for:
   - "Updating inventory" instead of "Updating variants"
   - Inventory quantities being set (not visibility toggles)

3. If sync completes without errors, the integration is working.

If there are API errors:
- Check WIX API key has inventory permissions
- Verify product ID format is correct for inventory API
- Check error messages for specific issues
  </action>
  <verify>npm run sync:run completes without errors, logs show inventory updates</verify>
  <done>Stock sync successfully updates WIX inventory quantities</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Stock visibility using WIX native inventory tracking instead of hidden variants</what-built>
  <how-to-verify>
    1. Open WIX store: https://hotboxclothing.shop
    2. Navigate to a product that has some out-of-stock variants (check monitor snapshots for a product with partial stock)
    3. On the product page, select colors and sizes
    4. For an in-stock combination: should show normally with "Add to Cart"
    5. For an out-of-stock combination: should show "Out of Stock" message (not hidden)
    6. Verify the out-of-stock variant selection is visible but marked as unavailable

    Alternative: If no products have mixed stock, create a test product:
    - Use preview server to create a product
    - Manually set one variant to qty=0 in WIX Dashboard > Store Products > Inventory
    - Check storefront displays "Out of Stock" for that variant
  </how-to-verify>
  <resume-signal>Type "approved" if out-of-stock variants show "Out of Stock" message, or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update OPERATIONS.md documentation</name>
  <files>scripts/OPERATIONS.md</files>
  <action>
Update OPERATIONS.md to document the new stock visibility behavior:

1. Find the "Stock Sync" section and update:

Change description from "updates WIX variant visibility" to "updates WIX inventory quantities"

2. Add new subsection explaining the behavior:

```markdown
### Stock Visibility Behavior (Phase 31)

**Previous behavior:** Out-of-stock variants were hidden (visible: false). Customers couldn't see that certain color/size combinations existed.

**Current behavior:** All variants remain visible. WIX Inventory API tracks stock quantities:
- In-stock variants: Show normally with "Add to Cart"
- Out-of-stock variants: Show "Out of Stock" message when selected

This improves UX by showing customers the full range of available options while clearly indicating which are unavailable.

**Technical change:**
- Stock sync uses `updateInventory()` instead of `updateProductVariants()` for stock changes
- Product creation enables `trackQuantity: true` and sets initial quantities
- Variant `visible` field remains `true` for all variants
```

3. Update any references to "hiding variants" or "variant visibility" to reflect new behavior.
  </action>
  <verify>OPERATIONS.md contains updated stock visibility documentation</verify>
  <done>Operations documentation reflects new inventory-based stock visibility</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Stock sync runs successfully with inventory API
- [ ] WIX storefront shows "Out of Stock" for unavailable variants (verified by human)
- [ ] OPERATIONS.md updated with new behavior documentation
</verification>

<success_criteria>
- All tasks completed
- Stock visibility verified in live storefront
- Operations documentation updated
- No hidden variants - all show with stock status
</success_criteria>

<output>
After completion, create `.planning/phases/31-stock-visibility/31-03-SUMMARY.md`
</output>
