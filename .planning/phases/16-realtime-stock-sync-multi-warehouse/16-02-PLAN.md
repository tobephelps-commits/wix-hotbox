---
phase: 16-realtime-stock-sync-multi-warehouse
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified: [scripts/monitor/alerts.ts, scripts/monitor/types.ts, scripts/sync/notifications.ts]
autonomous: true
---

<objective>
Add warehouse-aware stock alerts that detect when specific warehouses go out-of-stock or back-in-stock, and surface warehouse detail in email notifications.

Purpose: The store owner ships primarily from nearby warehouses. Knowing which warehouse lost stock (not just the aggregate) enables faster fulfillment decisions. Aggregate alerts remain the primary signal; warehouse alerts are supplementary detail.
Output: Alerts include warehouse breakdown context. Email notifications show which warehouses have stock and which don't.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/16-realtime-stock-sync-multi-warehouse/16-01-SUMMARY.md
@scripts/monitor/alerts.ts
@scripts/monitor/types.ts
@scripts/sync/notifications.ts
@scripts/sanmar/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add warehouse context to stock alerts</name>
  <files>scripts/monitor/alerts.ts, scripts/monitor/types.ts</files>
  <action>
  Extend the StockAlert type in types.ts to include an optional `warehouseDetail` field:
  ```
  warehouseDetail?: {
    warehousesWithStock: { id: number, name: string, qty: number }[]
    warehousesOutOfStock: { id: number, name: string }[]
    totalWarehouses: number
  }
  ```

  In alerts.ts, update detectAlerts() to populate warehouseDetail when the current SkuSnapshot has a `warehouses` array. For each alert generated:
  - Split warehouses into "with stock" (qty > 0) and "out of stock" (qty === 0 or not present)
  - Use WAREHOUSES constant from sanmar/constants.ts to identify warehouses not in the snapshot response (they're implicitly zero)
  - Set totalWarehouses to 9 (all SanMar warehouses)

  Update formatAlert() to include a one-line warehouse summary when warehouseDetail is present. Format: "Stock in: Seattle(450), Dallas(200). Out: Cincinnati, Reno, +5 more"
  - Show up to 3 warehouses with stock (sorted by qty descending)
  - Show up to 2 out-of-stock warehouses, then "+N more" for the rest

  Keep all existing alert transition logic unchanged — warehouseDetail is supplementary context on existing alerts, NOT a new alert trigger.
  </action>
  <verify>npx tsc --noEmit passes. Run a poll cycle with a tracked product and verify console output shows warehouse detail in alert formatting (if alerts are triggered). If no alerts trigger, verify the code compiles correctly.</verify>
  <done>StockAlert includes warehouseDetail. Alert formatting shows warehouse summary. Existing alert transition logic unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update email notifications with warehouse breakdown</name>
  <files>scripts/sync/notifications.ts</files>
  <action>
  Update buildSyncEmailBody() in notifications.ts to include warehouse detail in the STOCK CHANGES section when alerts have warehouseDetail populated.

  For each alert that has warehouseDetail, add a line below the alert:
  ```
  ⚠️ OUT OF STOCK: PC61 Ath. Maroon / 2XL (was: 15 units)
     Warehouses with stock: Seattle (450), Dallas (200), Jacksonville (100)
     Warehouses out: Cincinnati, Reno, Phoenix, +3 more
  ```

  Also add a WAREHOUSE INVENTORY section at the end of the email (before SUMMARY) that shows per-product warehouse totals. Group by product, show top 3 warehouses by quantity:
  ```
  WAREHOUSE INVENTORY
  ─────────────────────
  PC61 Port & Company Essential Tee:
    Seattle: 12,450  |  Dallas: 8,200  |  Jacksonville: 6,100  |  +6 warehouses
  ```

  This section uses the latest snapshots (not just alerts) — import and call loadLatestSnapshot from monitor/store to get current warehouse data for each synced product.

  Keep the existing email format intact — these are additions, not replacements. If warehouseDetail is not present on alerts (old format), fall back to current behavior.
  </action>
  <verify>npx tsc --noEmit passes. Review the notification module to confirm warehouse sections are conditionally rendered.</verify>
  <done>Email notifications include per-alert warehouse detail and per-product warehouse inventory summary. Backward compatible with alerts lacking warehouseDetail.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] StockAlert type includes optional warehouseDetail
- [ ] Alert formatting shows warehouse context
- [ ] Email body includes warehouse sections when data available
- [ ] Falls back gracefully when warehouse data not present
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Warehouse detail enriches existing alerts without changing trigger logic
- Email notifications include warehouse breakdown
- Backward compatible with pre-warehouse snapshots
  </success_criteria>

<output>
After completion, create `.planning/phases/16-realtime-stock-sync-multi-warehouse/16-02-SUMMARY.md`
</output>
