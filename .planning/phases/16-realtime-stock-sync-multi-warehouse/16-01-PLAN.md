---
phase: 16-realtime-stock-sync-multi-warehouse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/monitor/types.ts, scripts/monitor/store.ts, scripts/monitor/poller.ts, scripts/sanmar/services/inventory.ts]
autonomous: true
---

<objective>
Extend the inventory snapshot data model to preserve per-warehouse quantity breakdowns instead of summing all warehouses into a single totalQty. Update the monitor store and poller to persist and load warehouse-level data.

Purpose: Foundation for warehouse-aware alerts, closest-warehouse logic, and warehouse inventory dashboard. Currently warehouse data flows through the SanMar API but is discarded during snapshot creation.
Output: SkuSnapshot includes per-warehouse breakdown. Snapshots saved with full warehouse detail. Backward compatible with existing totalQty-based consumers.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@scripts/monitor/types.ts
@scripts/monitor/store.ts
@scripts/monitor/poller.ts
@scripts/sanmar/services/inventory.ts
@scripts/sanmar/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SkuSnapshot with per-warehouse breakdown</name>
  <files>scripts/monitor/types.ts, scripts/sanmar/services/inventory.ts</files>
  <action>
  Add a WarehouseQuantity type to monitor/types.ts:
  ```
  WarehouseQuantity {
    warehouseId: number
    warehouseName: string
    qty: number
  }
  ```

  Extend SkuSnapshot to include an optional `warehouses` field (array of WarehouseQuantity) alongside existing `totalQty` and `wellStocked`. The field is optional for backward compatibility — existing snapshots without it still work.

  In sanmar/services/inventory.ts, add a helper function `getWarehouseBreakdown(skuInventory: SkuInventory): WarehouseQuantity[]` that maps the existing `whse` array (WarehouseInventory[]) to the new WarehouseQuantity[] format, filtering out zero-quantity warehouses. Export it from the module.

  Do NOT modify the existing `getTotalQuantity`, `isInStock`, or `isWellStocked` functions — they remain unchanged.
  </action>
  <verify>npx tsc --noEmit passes with no errors</verify>
  <done>WarehouseQuantity type exported from monitor/types.ts. SkuSnapshot has optional warehouses field. getWarehouseBreakdown helper exported from inventory service.</done>
</task>

<task type="auto">
  <name>Task 2: Update poller to populate warehouse data in snapshots</name>
  <files>scripts/monitor/poller.ts, scripts/monitor/store.ts</files>
  <action>
  In poller.ts, update the snapshot creation logic inside pollOnce(). Currently it creates SkuSnapshot entries by summing warehouse quantities. Change it to also populate the new `warehouses` field using getWarehouseBreakdown() from the inventory service.

  The totalQty and wellStocked fields continue to be computed the same way (sum across all warehouses, check for cap). The warehouses field is populated alongside them — additive change only.

  In store.ts, no changes needed — JSON.stringify/parse handles the new field automatically. But verify that loadLatestSnapshot correctly deserializes the warehouses array from saved JSON files.

  Update the barrel export in scripts/monitor/index.ts to include the new WarehouseQuantity type.

  Do NOT change alert detection, CLI, or any other consumer — they continue using totalQty as before.
  </action>
  <verify>Run `npx tsx scripts/monitor/manage.ts poll` with a tracked product. Check the saved snapshot file in data/monitor/snapshots/ — it should now include a `warehouses` array on each SKU entry with warehouseId, warehouseName, and qty fields. The totalQty field should still be present and match the sum of warehouse quantities.</verify>
  <done>Poll cycle produces snapshots with per-warehouse breakdown. Existing totalQty/wellStocked fields unchanged. Old snapshots without warehouses field load without error.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] Monitor poll produces snapshots with warehouse breakdown
- [ ] Existing alert detection still works (uses totalQty, unaffected)
- [ ] Existing sync still works (uses totalQty, unaffected)
- [ ] Old snapshot files without warehouses field load without error
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- SkuSnapshot now includes per-warehouse data
- No breaking changes to existing consumers
- Snapshot JSON files include warehouse arrays
  </success_criteria>

<output>
After completion, create `.planning/phases/16-realtime-stock-sync-multi-warehouse/16-01-SUMMARY.md`
</output>
