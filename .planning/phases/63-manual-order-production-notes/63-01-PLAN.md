---
phase: 63-manual-order-production-notes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/008-item-notes.sql
  - src/orders/types.ts
  - src/orders/order-service.ts
  - ui/src/components/orders/types.ts
  - ui/src/components/orders/OrderCreateForm.tsx
  - ui/src/components/orders/OrderDetail.tsx
  - src/orders/production-sheet.ts
autonomous: true
---

<objective>
Add free-text production notes per order line item for decoration/production instructions, and display them on production sheet PDFs.

Purpose: Production staff need per-item instructions (e.g., "Left chest logo, 3-color", "Back print, large format") visible on production sheets alongside the color/size/qty tables.
Output: `notes` field on order items persisted in DB, editable in OrderCreateForm, visible in OrderDetail, and printed on production sheet PDFs below each product section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/db/migrations/002-orders.sql
@src/orders/types.ts
@src/orders/order-service.ts
@ui/src/components/orders/types.ts
@ui/src/components/orders/OrderCreateForm.tsx
@ui/src/components/orders/OrderDetail.tsx
@src/orders/production-sheet.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add notes column to order_items and update types + service</name>
  <files>src/db/migrations/008-item-notes.sql, src/orders/types.ts, src/orders/order-service.ts, ui/src/components/orders/types.ts</files>
  <action>
1. Create migration `008-item-notes.sql`:
   ```sql
   ALTER TABLE order_items ADD COLUMN notes TEXT;
   ```

2. In `src/orders/types.ts`, add `notes?: string` to the `OrderLineItem` interface (after `imageUrl`).

3. In `src/orders/order-service.ts`:
   - Add `notes: string | null` to the `ItemRow` interface (after `image_url`).
   - In `mapItemRow()`, add `notes: row.notes ?? undefined` to the returned object.
   - In `createOrder()`, update the `insertItem` prepared statement:
     - Add `notes` to the column list (after `image_url`)
     - Add a `?` placeholder
     - In the `insertItem.run()` call, add `item.notes ?? null` after `item.imageUrl ?? null`

4. In `ui/src/components/orders/types.ts`, add `notes?: string` to the `OrderLineItem` interface (after `imageUrl`).
  </action>
  <verify>
   - `npx tsc --noEmit` passes (backend)
   - `cd ui && npx tsc --noEmit` passes (frontend)
  </verify>
  <done>
   - Migration file exists at 008-item-notes.sql
   - `notes` field present in OrderLineItem interface (server + UI)
   - ItemRow includes `notes` column
   - mapItemRow maps notes correctly
   - createOrder INSERT includes notes column and parameter
  </done>
</task>

<task type="auto">
  <name>Task 2: Add notes input to OrderCreateForm and display in OrderDetail</name>
  <files>ui/src/components/orders/OrderCreateForm.tsx, ui/src/components/orders/OrderDetail.tsx</files>
  <action>
**OrderCreateForm.tsx:**
1. The `FormLineItem` interface already extends OrderLineItem fields. Ensure `notes` is included (it should flow through from the type update, but verify the `FormLineItem` interface explicitly includes it if it cherry-picks fields).
2. In the line item rendering section (where color, size, quantity, unit price inputs are shown), add a `textarea` input for notes:
   - Label: "Production Notes"
   - Placeholder: "e.g., Left chest logo, 3-color"
   - Single row textarea (rows={1}) that expands naturally
   - Full width below the other fields in the item card
   - Bind to `item.notes`, update via the same state update pattern used for other fields
3. When building the submission payload, include `notes` in each item object (already should flow through if FormLineItem includes it).

**OrderDetail.tsx:**
1. In the line items table, after the product name/variant info inside the `<td>` cell for each item, conditionally render the notes:
   ```tsx
   {item.notes && (
     <div className="order-detail__item-notes">
       {item.notes}
     </div>
   )}
   ```
2. Style `order-detail__item-notes` inline or via existing CSS patterns: smaller font size (0.85em), muted color (#666), italic, margin-top 2px. Use inline styles to match existing component patterns (OrderDetail uses inline styles for flex layouts).
  </action>
  <verify>
   - `cd ui && npx tsc --noEmit` passes
   - OrderCreateForm renders a "Production Notes" textarea per line item
   - OrderDetail shows notes below product name when present
  </verify>
  <done>
   - Notes textarea visible per line item in create form
   - Notes included in form submission payload
   - Notes displayed in order detail view when present
   - No notes shown when field is empty (no empty labels)
  </done>
</task>

<task type="auto">
  <name>Task 3: Display production notes on production sheet PDF</name>
  <files>src/orders/production-sheet.ts</files>
  <action>
1. In the `ProductGroup` interface, add `notes: string[]` to collect all unique notes from grouped line items.

2. In `groupLineItemsByStyle()`:
   - Initialize `notes: []` in the group creation
   - When iterating items, if `item.notes` is truthy and not already in the group's notes array, push it: `if (item.notes && !group.notes.includes(item.notes)) group.notes.push(item.notes);`

3. In `drawProductSection()`, after the vendor badge line (around line 192) and before the quantities table header (line 205), add a notes block:
   ```
   if (group.notes.length > 0) {
     const notesY = sectionStartY + (group.vendor ? 32 : 18);
     doc
       .font('Helvetica-Oblique')
       .fontSize(FONT_SIZE_SMALL)
       .fillColor('#444444');
     for (const note of group.notes) {
       doc.text(`Notes: ${note}`, PAGE_MARGIN + 5, notesY);
       // Advance Y for each note line
     }
   }
   ```
   Adjust `tableStartY` calculation to account for notes height: add `(group.notes.length * 14)` to the offset so the table starts below the notes.

4. Ensure page break logic in `drawProductSection` accounts for notes height: increase the minimum space check from 200 to `200 + (group.notes.length * 14)`.
  </action>
  <verify>
   - `npx tsc --noEmit` passes
   - Production sheet PDF generation still works (no runtime errors)
   - Notes appear in italic below product name/vendor on the production sheet
  </verify>
  <done>
   - ProductGroup includes notes array
   - groupLineItemsByStyle collects unique notes per product group
   - drawProductSection renders notes in italic below vendor badge, above quantities table
   - Table layout adjusts vertically to accommodate notes
   - Page break logic accounts for notes height
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes (backend)
- [ ] `cd ui && npx tsc --noEmit` passes (frontend)
- [ ] Migration file 008-item-notes.sql exists with ALTER TABLE
- [ ] Notes field flows end-to-end: form → API → DB → detail view → production sheet PDF
- [ ] No regressions: orders without notes work unchanged
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors introduced
- Production notes field persisted in database
- Notes editable per line item during order creation
- Notes visible in order detail view
- Notes printed on production sheet PDFs
- Existing orders without notes display and print normally
  </success_criteria>

<output>
After completion, create `.planning/phases/63-manual-order-production-notes/63-01-SUMMARY.md`
</output>
