---
phase: 47-product-pipeline-creation-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/types.ts
  - src/pipeline/pricing-rules.ts
  - src/pipeline/mapper.ts
  - src/pipeline/wix-api.ts
  - src/pipeline/templates.ts
  - src/pipeline/index.ts
  - src/config.ts
autonomous: true
---

<objective>
Port pipeline business logic modules from scripts/pipeline/ to src/pipeline/ for the v2.0 backend architecture.

Purpose: Establish the core pipeline data layer that API routes and UI will consume -- pricing rules, WIX type definitions, product-to-WIX mapper, WIX REST API client, and template system.
Output: src/pipeline/ directory with all business logic modules, compilable with zero errors.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-backend-architecture/44-02-SUMMARY.md
@.planning/phases/46-product-pipeline-vendor-apis/46-01-SUMMARY.md

@src/config.ts
@src/vendors/types.ts
@src/vendors/registry.ts
@scripts/pipeline/types.ts
@scripts/pipeline/pricing-rules.ts
@scripts/pipeline/mapper.ts
@scripts/pipeline/wix-api.ts
@scripts/pipeline/templates.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port pricing-rules.ts, pipeline types, and mapper to src/pipeline/</name>
  <files>src/pipeline/pricing-rules.ts, src/pipeline/types.ts, src/pipeline/mapper.ts</files>
  <action>
Port these three modules from scripts/pipeline/ to src/pipeline/:

1. **pricing-rules.ts** — Copy as-is. Pure business logic, no external dependencies. Contains PricingConfig, PRICING_PRESETS, calculateRetailPrice, calculateVariantPrice, calculateMargin, calculateFullMargin, calculateTotalCost.

2. **types.ts** — Port with import path updates:
   - Change `import type { PricingConfig } from './pricing-rules.js'` (same — stays local)
   - Change `import type { VendorId } from '../vendor/types.js'` to `import type { VendorId } from '../vendors/types.js'`
   - Re-export both PricingConfig and VendorId as before
   - Keep ALL type definitions: WIX V1 types (WixCreateProductRequest, WixMediaItem, WixVariantUpdate, etc.), pipeline intermediates (CuratedProduct, CuratedColor, ProductPreview, ColorPreview, PricingPreview), logo types, cost tracking types, batch types, sale types, coupon types, inventory types.

3. **mapper.ts** — Port with significant refactoring:
   - Remove all SanMar-specific imports (ProductInfo, PricingInfo, MediaContent, SkuInventory, and SanMar service functions)
   - Rewrite buildProductPreview to accept UnifiedProductData (from src/vendors/types.ts) instead of raw SanMar types. Map UnifiedProduct[] to ColorPreview[] using UnifiedMedia[] for images and UnifiedInventory[] for stock status.
   - Rewrite buildVariantUpdates to accept UnifiedProductData instead of SanMar types. Use UnifiedPricing[] for wholesale cost lookup and UnifiedProduct[] for weight lookup.
   - buildCreateProductPayload stays mostly the same (takes CuratedProduct, no vendor-specific deps)
   - buildMediaPayload: Rewrite to accept CuratedProduct + UnifiedMedia[] instead of Map<string, MediaContent[]>. Build WixMediaItem[] from UnifiedMedia frontImage/backImage URLs assigned to Color choices.
   - buildInventoryUpdate: Rewrite to accept UnifiedInventory[] instead of SkuInventory[]. Use totalQty from UnifiedInventory.
   - Keep calculateRetailPrice and calculateVariantPrice imports from local pricing-rules.ts

  Key principle: mapper.ts should ONLY depend on src/pipeline/types.ts, src/pipeline/pricing-rules.ts, and src/vendors/types.ts. Zero SanMar-specific or S&S-specific imports.
  </action>
  <verify>npx tsc --noEmit passes with zero errors</verify>
  <done>src/pipeline/pricing-rules.ts, types.ts, and mapper.ts exist, compile cleanly, and mapper uses unified vendor types exclusively</done>
</task>

<task type="auto">
  <name>Task 2: Port WIX API client and template system to src/pipeline/</name>
  <files>src/pipeline/wix-api.ts, src/pipeline/templates.ts, src/pipeline/index.ts, src/config.ts</files>
  <action>
1. **src/config.ts** — Add WIX API credentials:
   - Add `wixApiKey: string | undefined` and `wixSiteId: string | undefined` to Config interface
   - Load from process.env.WIX_API_KEY and process.env.WIX_SITE_ID
   - WIX_SITE_ID default to 'c744cbdb-46f8-4c66-ac76-eb31bd0d52c1' (from Phase 1 audit, hardcoded in v1.x)

2. **wix-api.ts** — Port from scripts/pipeline/wix-api.ts with these changes:
   - Remove `import 'dotenv/config'` (loaded once at startup)
   - Remove hardcoded WIX_SITE_ID and WIX_API_KEY from env — instead, accept a config object parameter or use a module-level init pattern. Use a `setWixConfig(config: { apiKey: string; siteId: string })` function that route handlers call at registration time, storing in module-level variables. This avoids importing fastify into a pure API client.
   - Remove the CLI runner block at the bottom (fileURLToPath / process.argv)
   - Remove `import { writeFileSync } from 'fs'` (CLI-only)
   - Keep ALL public functions: createProduct, addProductMedia, updateProductVariants, updateProduct, updateProductPrice, getProduct, addProductToCollection, queryProducts, listAllProducts, listCollections, getCollectionByName, getInventory, updateInventory
   - Keep ALL response types: WixProductPageUrl, WixVariantStock, WixVariantPriceData, WixVariantData, WixVariant, WixProduct, WixCollection, WixInventoryItemVariant, WixInventoryItem
   - Keep getHeaders() and handleErrorResponse() internal helpers

3. **templates.ts** — Port from scripts/pipeline/templates.ts with these changes:
   - Remove `fileURLToPath` path resolution — instead accept a data directory path via `setTemplatesDir(dir: string)` init function (route handlers pass fastify.config.dataDir)
   - Templates file at `{dataDir}/templates.json`
   - Keep all functions: loadTemplates, saveTemplate, getTemplate, deleteTemplate, listTemplates

4. **index.ts** — Barrel export for src/pipeline/:
   - Re-export everything from pricing-rules.ts, types.ts, mapper.ts
   - Re-export public functions and types from wix-api.ts
   - Re-export public functions from templates.ts
  </action>
  <verify>npx tsc --noEmit passes with zero errors</verify>
  <done>src/pipeline/wix-api.ts, templates.ts, and index.ts exist and compile cleanly. Config includes WIX credentials. All pipeline modules are importable from src/pipeline/index.ts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes with zero errors
- [ ] All src/pipeline/*.ts files exist (pricing-rules, types, mapper, wix-api, templates, index)
- [ ] mapper.ts imports ONLY from src/vendors/types.ts and local pipeline modules (no scripts/ imports)
- [ ] wix-api.ts has no hardcoded credentials or dotenv imports
- [ ] templates.ts uses configurable data directory
</verification>

<success_criteria>
- All pipeline business logic ported to src/pipeline/
- mapper.ts uses UnifiedProductData instead of SanMar-specific types
- WIX API client uses config-based credentials
- Template system uses configurable data directory
- Zero TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/47-product-pipeline-creation-ui/47-01-SUMMARY.md`
</output>
