---
phase: 47-product-pipeline-creation-ui
plan: 03
type: execute
wave: 3
depends_on: ["47-02"]
files_modified:
  - ui/src/components/products/ProductsTab.tsx
  - ui/src/components/products/StyleLookup.tsx
  - ui/src/components/products/ProductPreview.tsx
  - ui/src/components/products/ColorCard.tsx
  - ui/src/components/products/SizeChips.tsx
  - ui/src/components/products/ProductsTab.css
  - ui/src/components/ContentArea.tsx
autonomous: true
---

<objective>
Build the Products tab UI with style lookup form and product preview display.

Purpose: Replace the placeholder "Coming in Phase 47" content with a functional style lookup that queries vendor APIs and displays product data with color cards, size chips, and stock badges.
Output: Working Products tab with style lookup -> preview flow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-touch-ui-foundation/45-02-SUMMARY.md
@.planning/phases/47-product-pipeline-creation-ui/47-02-SUMMARY.md

@ui/src/App.tsx
@ui/src/components/ContentArea.tsx
@ui/src/components/Sidebar.tsx
@ui/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build style lookup form with vendor selector</name>
  <files>ui/src/components/products/ProductsTab.tsx, ui/src/components/products/StyleLookup.tsx, ui/src/components/products/ProductsTab.css, ui/src/components/ContentArea.tsx</files>
  <action>
1. **ContentArea.tsx** — Replace placeholder for products tab:
   - Import ProductsTab component
   - Render `<ProductsTab />` when activeTab === 'products' instead of the placeholder div

2. **ProductsTab.tsx** — Main container for the products workflow:
   - State machine with steps: 'lookup' | 'preview' | 'configure' | 'creating' | 'done'
   - Renders StyleLookup when step is 'lookup'
   - Renders ProductPreview (Task 2) when step is 'preview'
   - Steps 'configure', 'creating', 'done' render placeholders (Plan 04)
   - Holds state: currentPreview (ProductPreview data), selectedVendor, selectedStyle, rawData reference
   - Back button to return to lookup from preview

3. **StyleLookup.tsx** — Style number entry form:
   - Input field for style number (text, auto-uppercase, touch-friendly 48px height)
   - Vendor selector: two large touch-friendly buttons for "SanMar" and "S&S" (default SanMar)
   - "Look Up" button (disabled when style is empty, shows spinner while loading)
   - On submit: fetch GET /api/pipeline/preview/{vendorId}/{style}
   - Handle errors: show inline error message (red text below form) for API errors
   - On success: call onPreviewReady(preview, vendorId, style) prop to advance to preview step

4. **ProductsTab.css** — Styles for the products workflow:
   - Dark theme consistent with existing app (use CSS custom properties)
   - Touch-friendly inputs (min 44px touch targets)
   - Vendor selector buttons: large pill buttons, active state highlighted with accent color
   - Loading spinner (CSS animation, no external dependency)
   - Error message styling
  </action>
  <verify>npm run build (from ui/) succeeds. Products tab shows style lookup form with vendor selector and look up button.</verify>
  <done>StyleLookup renders in Products tab, vendor selector toggles, form submits to API, errors display inline</done>
</task>

<task type="auto">
  <name>Task 2: Build product preview with color cards, size chips, and stock badges</name>
  <files>ui/src/components/products/ProductPreview.tsx, ui/src/components/products/ColorCard.tsx, ui/src/components/products/SizeChips.tsx</files>
  <action>
1. **ProductPreview.tsx** — Product preview display after successful lookup:
   - Header: brand name, product title, style number
   - Pricing summary bar: wholesale price, suggested retail, sale badge (if active)
   - Color cards grid (2 columns on desktop, 1 on mobile)
   - Size selection chips below color grid
   - "Continue to Pricing" button at bottom (advances to 'configure' step — implemented in Plan 04)
   - State: selectedColors (Set of catalogColor strings, initialized to all in-stock colors), selectedSizes (Set of size strings, initialized to all sizes)
   - Toggle color selection by clicking color cards
   - Toggle size selection by clicking size chips
   - Select All / Deselect All buttons for both colors and sizes
   - Disable "Continue" button if no colors or no sizes selected

2. **ColorCard.tsx** — Individual color card component:
   - Swatch image (or colored placeholder if no swatch URL)
   - Front product image thumbnail (120px, or placeholder)
   - Color name text
   - Stock badge: green "In Stock" or red "Out of Stock" or yellow "Unknown"
   - Selected state: border highlight with accent color
   - Touch-friendly: entire card is tappable (min 44px)
   - Dim/opacity reduction for out-of-stock colors (still selectable)

3. **SizeChips.tsx** — Size selection chip row:
   - Horizontal scrollable row of size chips (S, M, L, XL, 2XL, etc.)
   - Each chip: pill button, selected state filled with accent color
   - Touch-friendly: min 44px height, 8px gap between chips
   - Momentum scrolling for overflow on small screens
  </action>
  <verify>npm run build (from ui/) succeeds. After entering a style number and clicking Look Up, the preview displays color cards with images and size chips.</verify>
  <done>ProductPreview renders color cards with swatch/front images, stock badges, and toggleable size chips. Color and size selection state managed correctly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` from ui/ succeeds
- [ ] `npx tsc --noEmit` from root succeeds
- [ ] Products tab shows StyleLookup form (not placeholder)
- [ ] Vendor selector toggles between SanMar and S&S
- [ ] Style lookup form calls API and displays loading state
- [ ] ProductPreview renders color cards with images and stock badges
- [ ] Size chips are toggleable with selected/deselected state
- [ ] Color cards are toggleable with visual selected state
- [ ] Select All / Deselect All buttons work for colors and sizes
</verification>

<success_criteria>
- Products tab is functional with style lookup and preview
- Touch-friendly UI with 44px minimum targets
- Dark theme consistent with existing app
- Error handling for API failures
- Color and size selection state managed in ProductsTab
</success_criteria>

<output>
After completion, create `.planning/phases/47-product-pipeline-creation-ui/47-03-SUMMARY.md`
</output>
