---
phase: 42-integration-polish
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - scripts/OPERATIONS.md
autonomous: false
---

<objective>
Update OPERATIONS.md with v1.2 features and perform visual verification of the complete dashboard.

Purpose: Document all new v1.2 capabilities in the operations runbook and verify the integrated dashboard UX works correctly.
Output: Updated OPERATIONS.md with S&S cart, production sheets, bulk operations; human-verified dashboard.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# v1.2 features to document:
# - S&S cart automation (npm run ss-cart, ss-cart:preview, ss-cart:fill)
# - Production sheets (API endpoint, bulk download)
# - Order status dashboard (extended summary, aging orders, attention indicators)
# - Bulk order actions (multi-select, batch status, batch production sheets)
# - Tabbed dashboard navigation

@scripts/OPERATIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add S&S Cart Automation section to OPERATIONS.md</name>
  <files>scripts/OPERATIONS.md</files>
  <action>
Add S&S cart automation commands to the Quick Reference section, after the existing "SanMar Cart Automation" section:

```markdown
### S&S Activewear Cart Automation

| Command | What it does |
|---------|-------------|
| `npm run ss-cart` | Preview S&S cart items (no action taken) |
| `npm run ss-cart:preview` | Preview S&S cart items (alias) |
| `npm run ss-cart:fill` | Execute S&S cart fill automation |
```

Also add a workflow section "Filling an S&S Cart" in Common Workflows that mirrors the SanMar cart workflow pattern.
  </action>
  <verify>grep "ss-cart" scripts/OPERATIONS.md returns matches</verify>
  <done>S&S cart commands documented in Quick Reference and Common Workflows</done>
</task>

<task type="auto">
  <name>Task 2: Add Production Sheets and Bulk Operations documentation</name>
  <files>scripts/OPERATIONS.md</files>
  <action>
Add production sheets and bulk operations to the Order Management section:

1. In Quick Reference under Order Management, add:
```markdown
| `npm run preview` → Orders tab | Access production sheets, bulk operations |
```

2. Add a new workflow section "Bulk Order Operations" in Common Workflows:
```markdown
### Bulk Order Operations

The dashboard Orders tab supports multi-select operations:

1. **Open Orders tab** in the dashboard (`npm run preview`)
2. **Select orders** using checkboxes in the order table
3. **Use the bulk action toolbar** that appears at bottom:
   - **Apply Status**: Change status for all selected orders
   - **Production Sheets**: Download ZIP of production sheet PDFs
   - **Fill SanMar/S&S Cart**: Pre-populate with selected order items

**Production Sheet per Order:**
Access individual production sheets via the API:
```bash
curl http://localhost:3456/api/orders/{id}/production-sheet -o production-sheet.pdf
```
```
  </action>
  <verify>grep "Bulk Order" scripts/OPERATIONS.md returns a match</verify>
  <done>Production sheets and bulk operations documented</done>
</task>

<task type="auto">
  <name>Task 3: Document tabbed dashboard navigation</name>
  <files>scripts/OPERATIONS.md</files>
  <action>
Update the Preview Server section to reflect the new tabbed navigation:

Find the section describing the preview server UI and update to mention:

```markdown
### Dashboard Navigation

The preview server dashboard is organized into four tabs:

| Tab | Contents |
|-----|----------|
| **Products** | Product creation wizard, color/size selection, logo placement, batch operations |
| **Orders** | Order list, status management, invoices, labels, production sheets, cart automation, bulk actions |
| **Inventory** | Tracked products, stock alerts, sync health, per-product thresholds |
| **Customers** | Customer accounts, pricing markup, logo assignments, royalty statements |

The Operations Dashboard (daemon controls, system health) remains visible at all times above the tabs.
```
  </action>
  <verify>grep "Dashboard Navigation" scripts/OPERATIONS.md returns a match</verify>
  <done>Tabbed navigation documented with tab contents</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete v1.2 dashboard with tabbed navigation, production sheets, S&S cart automation, order status pipeline, and bulk operations</what-built>
  <how-to-verify>
    1. Run: `npm run preview`
    2. Open: http://localhost:3456

    **Verify tabbed navigation:**
    3. Sidebar shows 4 tabs: Products, Orders, Inventory, Customers
    4. Click each tab - content switches correctly
    5. Operations Dashboard visible at top (not affected by tabs)
    6. Refresh page - last active tab is restored

    **Verify Orders tab features:**
    7. Navigate to Orders tab
    8. Order table shows checkbox column
    9. Select 2-3 orders - bulk action toolbar appears at bottom
    10. Toolbar shows: status dropdown, "Production Sheets" button, cart fill buttons
    11. Click "Production Sheets" - ZIP downloads (may be empty if no valid orders)
    12. Both "Fill SanMar Cart" (orange) and "Fill S&S Cart" (teal) buttons visible

    **Verify S&S cart modal:**
    13. Click "Fill S&S Cart" button
    14. Modal opens with "Fill S&S Activewear Cart" header
    15. Preview table shows or indicates no S&S items

    **Verify order status dashboard (if orders exist):**
    16. Order counts visible in status summary
    17. Attention indicators highlight orders needing action

    **Responsive check:**
    18. Resize browser to 768px width - sidebar collapses
  </how-to-verify>
  <resume-signal>Type "approved" to complete v1.2 milestone, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] OPERATIONS.md includes S&S cart commands
- [ ] OPERATIONS.md includes bulk operations workflow
- [ ] OPERATIONS.md includes tabbed navigation documentation
- [ ] Human verification checkpoint approved
</verification>

<success_criteria>
- All tasks completed
- OPERATIONS.md fully documents v1.2 features
- Dashboard verified working end-to-end
- v1.2 milestone ready for completion
</success_criteria>

<output>
After completion, create `.planning/phases/42-integration-polish/42-02-SUMMARY.md`
</output>
