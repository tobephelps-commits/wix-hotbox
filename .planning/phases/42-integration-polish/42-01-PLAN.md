---
phase: 42-integration-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/smoke-test.ts
autonomous: true
---

<objective>
Update smoke-test.ts with v1.2 API endpoints and CLI commands for comprehensive integration validation.

Purpose: Ensure all new v1.2 features (tabbed navigation, production sheets, S&S cart automation, order status dashboard, bulk operations) have automated validation coverage.
Output: Extended smoke-test.ts that validates all v1.2 endpoints and commands.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# v1.2 features requiring smoke test coverage:
# - Phase 37: Tabbed dashboard (UI only, no new endpoints)
# - Phase 38: Production sheets (/api/orders/:id/production-sheet)
# - Phase 39: S&S cart (/api/ss-cart/preview, /api/ss-cart/fill, /api/ss-cart/history)
# - Phase 40: Extended order summary (/api/orders/summary/extended)
# - Phase 41: Bulk operations (/api/orders/bulk/status, /api/orders/bulk/production-sheets)

@scripts/smoke-test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v1.2 API endpoints to smoke test</name>
  <files>scripts/smoke-test.ts</files>
  <action>
Add the following endpoints to the ENDPOINT_TESTS array:

```typescript
// v1.2 endpoints
{ path: '/api/orders/summary/extended', description: 'Extended order summary with aging', expectJson: true },
{ path: '/api/ss-cart/preview', description: 'S&S cart preview', expectJson: true },
{ path: '/api/ss-cart/history', description: 'S&S cart fill history', expectJson: true },
```

Note: Production sheet and bulk operation endpoints require order IDs, so they cannot be tested with simple GET requests. The bulk endpoints are POST-only. Focus on the GET endpoints that work without parameters.

Update the comment header to indicate v1.2 validation.
  </action>
  <verify>grep -c "v1.2" scripts/smoke-test.ts returns at least 1</verify>
  <done>ENDPOINT_TESTS array includes ss-cart and orders/summary/extended endpoints</done>
</task>

<task type="auto">
  <name>Task 2: Add v1.2 CLI commands to smoke test</name>
  <files>scripts/smoke-test.ts</files>
  <action>
Add the following CLI tests to the CLI_TESTS array:

```typescript
{
  name: 'ss-cart-cli.ts help',
  cmd: 'npx',
  args: ['tsx', 'scripts/orders/ss-cart-cli.ts', 'help'],
},
```

This validates that the S&S cart CLI (added in Phase 39) loads and displays help correctly.
  </action>
  <verify>grep "ss-cart-cli" scripts/smoke-test.ts returns a match</verify>
  <done>CLI_TESTS array includes S&S cart CLI validation</done>
</task>

<task type="auto">
  <name>Task 3: Run smoke test and verify all pass</name>
  <files>scripts/smoke-test.ts</files>
  <action>
Run the smoke test to verify all existing and new checks pass:

```bash
npm run smoke-test
```

If any tests fail, investigate and fix the issue (may be a pre-existing issue or new endpoint returning unexpected format).

Expected result: All tests PASS or WARN (WARN is acceptable for env-var-dependent tests).
  </action>
  <verify>npm run smoke-test exits with code 0</verify>
  <done>All smoke test checks pass or warn, zero FAIL results for new endpoints</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run smoke-test` exits with code 0
- [ ] New v1.2 endpoints appear in smoke test output
- [ ] S&S cart CLI test appears in smoke test output
</verification>

<success_criteria>
- All tasks completed
- Smoke test covers all testable v1.2 endpoints
- Zero regressions in existing smoke tests
</success_criteria>

<output>
After completion, create `.planning/phases/42-integration-polish/42-01-SUMMARY.md`
</output>
