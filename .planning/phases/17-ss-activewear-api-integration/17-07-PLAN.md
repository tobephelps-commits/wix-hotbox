---
phase: 17-ss-activewear-api-integration
plan: 07
type: execute
wave: 4
depends_on: ["17-05", "17-06"]
files_modified: [scripts/preview/server.ts, scripts/preview/public/index.html]
autonomous: true
user_setup: []
---

<objective>
Add vendor awareness to the preview server UI and API endpoints.

Purpose: The preview server is the single interface for product curation. It must display vendor source, support vendor selection, and show vendor-specific details (e.g., MAP price for S&S, price code for SanMar).
Output: Preview server shows vendor info, API endpoints accept vendor parameter.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-ss-activewear-api-integration/17-05-SUMMARY.md
@.planning/phases/17-ss-activewear-api-integration/17-06-SUMMARY.md

# Source files:
@scripts/preview/server.ts
@scripts/preview/public/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vendor parameter to preview server API endpoints</name>
  <files>scripts/preview/server.ts</files>
  <action>
Read the existing preview server to understand its API endpoints and how it serves product data.

Add vendor support to relevant API endpoints:

1. **Product fetch endpoint** (the endpoint that fetches SanMar product data for preview):
   - Accept `vendor` query parameter: `?vendor=ss` or `?vendor=sanmar`
   - Default to 'sanmar' if not provided (backward compatible)
   - Pass vendor to fetchProductData(style, vendor)
   - Return vendor info in response JSON

2. **Product creation endpoint** (if it exists):
   - Accept `vendor` in request body
   - Pass to create pipeline

3. **Inventory/monitoring endpoints** (if they exist):
   - Include vendor in tracked product data returned to frontend
   - Filter by vendor if `?vendor=` query param provided

4. **Template endpoints** — no change needed (templates are vendor-agnostic per CONTEXT.md)

5. Import both adapters at server startup to ensure registration:
   ```typescript
   import '../sanmar/adapter.js';
   import '../ss-activewear/adapter.js';
   ```

Ensure all existing endpoints continue working without vendor parameter (backward compatible).
  </action>
  <verify>npx tsc --noEmit scripts/preview/server.ts</verify>
  <done>Preview API endpoints accept vendor parameter, default to SanMar, return vendor info in responses</done>
</task>

<task type="auto">
  <name>Task 2: Add vendor display to preview server UI</name>
  <files>scripts/preview/public/index.html</files>
  <action>
Read the existing preview server HTML/JS to understand the UI structure.

Add vendor awareness to the frontend:

1. **Vendor selector** — Add a dropdown/toggle near the style input:
   - Options: "SanMar" (default), "S&S Activewear"
   - When vendor changes, pass it as query parameter in API calls
   - Style: consistent with existing UI (follow existing CSS patterns)

2. **Vendor badge** — Show vendor source on product preview:
   - After product loads, display a small badge/label showing "SanMar" or "S&S Activewear"
   - Use vendor-appropriate styling (different subtle color/label)

3. **Vendor-specific pricing info:**
   - For SanMar: show Price Code if available (existing behavior)
   - For S&S: show MAP Price and Customer Price if they differ from piece price
   - Add a small "Pricing" detail section that adapts based on vendor

4. **Inventory section** — Update warehouse display:
   - SanMar warehouses show by name (Seattle, Dallas, etc.)
   - S&S warehouses show by state abbreviation (IL, TX, etc.) with full location name
   - The existing warehouse breakdown UI should work for both — just different labels

5. **Monitor section** (if it shows tracked products):
   - Add vendor column/indicator to tracked products list
   - Vendor selector for "Add to monitoring" action

Keep changes minimal and consistent with existing UI patterns. Do NOT redesign the UI — just add vendor information where relevant.
  </action>
  <verify>Start preview server and verify it loads without JavaScript errors: npx tsx scripts/preview/server.ts (then check for startup success)</verify>
  <done>Preview server shows vendor selector, vendor badge on products, vendor-specific pricing details, warehouse labels adapt to vendor</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes for preview server files
- [ ] Preview server starts without errors
- [ ] API endpoints accept ?vendor=ss parameter
- [ ] API endpoints default to SanMar without vendor parameter
- [ ] UI shows vendor selector dropdown
- [ ] Product preview displays vendor badge
- [ ] Pricing section shows vendor-specific fields
- [ ] Warehouse labels correct for both vendors
- [ ] No breaking changes to existing preview server behavior
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Preview server is fully vendor-aware
- Existing SanMar workflow unaffected
- S&S products viewable in preview UI
</success_criteria>

<output>
After completion, create `.planning/phases/17-ss-activewear-api-integration/17-07-SUMMARY.md`
</output>
