---
phase: 51-inventory-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/004-stock-sync.sql
  - src/sync/types.ts
  - src/sync/product-map.ts
  - src/sync/notifications.ts
  - src/sync/index.ts
autonomous: true
---

<objective>
Port stock sync foundation: database schema, types, product mapping store, and email notifications.

Purpose: Create the sync module's data layer and notification system -- product mappings link vendor styles to WIX product IDs, and notifications deliver sync digests via SMTP.
Output: SQLite migration for sync tables, type definitions, product mapping CRUD with SQLite, email notification sender, and barrel exports.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# V1.x source files to port from:
@scripts/sync/types.ts
@scripts/sync/product-map.ts
@scripts/sync/notifications.ts

# V2.0 infrastructure:
@src/db/index.ts
@src/config.ts
@src/vendors/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sync migration, port types and product-map</name>
  <files>src/db/migrations/004-stock-sync.sql, src/sync/types.ts, src/sync/product-map.ts</files>
  <action>
  Create SQLite migration `004-stock-sync.sql` with tables:

  1. `product_mappings` table:
     - id INTEGER PRIMARY KEY AUTOINCREMENT
     - style TEXT NOT NULL
     - vendor TEXT NOT NULL DEFAULT 'sanmar'
     - wix_product_id TEXT NOT NULL
     - product_name TEXT NOT NULL
     - linked_at TEXT NOT NULL DEFAULT (datetime('now'))
     - UNIQUE(style, vendor)

  Add index: product_mappings(wix_product_id).

  Port `src/sync/types.ts` from `scripts/sync/types.ts`:
  - Import VendorId from `../vendors/types.js`
  - Keep interfaces: ProductMapping, SyncResult, MappingAuditResult, NotificationResult, NotificationConfig
  - SyncConfig: replace `dataDir` with `db: Database` parameter pattern. Actually, keep SyncConfig minimal -- just NotificationConfig. The db is passed as function parameter per v2.0 conventions.
  - Add `SyncHealth` interface here (was inline in sync-poller.ts in v1.x):
    - startedAt: string, lastTickAt: string | null, lastSuccessAt: string | null
    - consecutiveErrors: number, totalCycles: number, totalErrors: number
    - productsPolled: number, lastTickDurationMs: number
    - avgTickDurationMs: number, maxTickDurationMs: number
    - notificationsSent: number, notificationsFailed: number

  Port `src/sync/product-map.ts` from `scripts/sync/product-map.ts`, converting to SQLite:
  - `findMapping(db, style, vendor?)`: SELECT from product_mappings WHERE style AND vendor
  - `saveMapping(db, mapping)`: INSERT OR REPLACE into product_mappings
  - `removeMapping(db, style, vendor?)`: DELETE from product_mappings
  - `listMappings(db)`: SELECT all from product_mappings, return ProductMapping[]
  - All functions take Database as first parameter per v2.0 pattern
  </action>
  <verify>npx tsc --noEmit --pretty 2>&1 | head -20 (types and product-map compile)</verify>
  <done>Migration file exists, types adapted for v2.0, product-map uses SQLite with Database parameter</done>
</task>

<task type="auto">
  <name>Task 2: Port email notifications and create barrel exports</name>
  <files>src/sync/notifications.ts, src/sync/index.ts</files>
  <action>
  Port `src/sync/notifications.ts` from `scripts/sync/notifications.ts`:
  - `sendSyncNotification(config, results, alerts?, auditResult?)`: Send SMTP email with sync digest
  - Uses nodemailer (already in package.json from v1.x, verify and add if missing)
  - Build email body with: sync summary (products synced, variants updated), alert summary (threshold crossings), audit results (orphaned mappings)
  - Return NotificationResult { success, error? }
  - If config.enabled is false, return { success: true } immediately (no-op)
  - Handle SMTP errors gracefully, never throw

  Check if nodemailer is in package.json. If not, run `npm install nodemailer` and `npm install -D @types/nodemailer`.

  Create `src/sync/index.ts` barrel export:
  - Re-export all types from types.ts
  - Re-export product-map functions
  - Re-export notifications functions
  </action>
  <verify>npx tsc --noEmit --pretty 2>&1 | head -20 (all files compile)</verify>
  <done>Notifications send SMTP email with sync digest, barrel exports complete, nodemailer available</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes with no errors
- [ ] Migration follows existing pattern
- [ ] All functions accept Database as first parameter (except notifications which takes NotificationConfig)
- [ ] Barrel index exports complete public API
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Sync module foundation ready for stock-sync engine and daemon (plan 04)
</success_criteria>

<output>
After completion, create `.planning/phases/51-inventory-sync/51-02-SUMMARY.md`
</output>
