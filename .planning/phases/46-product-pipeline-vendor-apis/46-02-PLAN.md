---
phase: 46-product-pipeline-vendor-apis
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - src/vendors/sanmar/types/auth.ts
  - src/vendors/sanmar/types/product.ts
  - src/vendors/sanmar/types/pricing.ts
  - src/vendors/sanmar/types/inventory.ts
  - src/vendors/sanmar/types/media.ts
  - src/vendors/sanmar/types/index.ts
  - src/vendors/sanmar/constants.ts
  - src/vendors/sanmar/auth.ts
  - src/vendors/sanmar/client.ts
  - src/vendors/sanmar/services/product.ts
  - src/vendors/sanmar/services/pricing.ts
  - src/vendors/sanmar/services/inventory.ts
  - src/vendors/sanmar/services/media.ts
  - src/vendors/sanmar/services/index.ts
  - src/vendors/sanmar/utils/error-handler.ts
  - src/vendors/sanmar/utils/retry.ts
  - src/vendors/sanmar/index.ts
autonomous: true
---

<objective>
Port the SanMar SOAP API client from scripts/sanmar/ into src/vendors/sanmar/.

Purpose: Make SanMar product data, pricing, inventory, and media queries available as importable modules in the v2.0 backend architecture.
Output: Complete SanMar client library at src/vendors/sanmar/ with all types, auth, SOAP client factory, service functions, and error handling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-product-pipeline-vendor-apis/46-01-SUMMARY.md

@scripts/sanmar/types/auth.ts
@scripts/sanmar/types/product.ts
@scripts/sanmar/types/pricing.ts
@scripts/sanmar/types/inventory.ts
@scripts/sanmar/types/media.ts
@scripts/sanmar/types/index.ts
@scripts/sanmar/constants.ts
@scripts/sanmar/auth.ts
@scripts/sanmar/client.ts
@scripts/sanmar/services/product.ts
@scripts/sanmar/services/pricing.ts
@scripts/sanmar/services/inventory.ts
@scripts/sanmar/services/media.ts
@scripts/sanmar/services/index.ts
@scripts/sanmar/index.ts
@src/vendors/types.ts
@src/vendors/registry.ts
@src/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port SanMar types, constants, auth, and SOAP client</name>
  <files>src/vendors/sanmar/types/auth.ts, src/vendors/sanmar/types/product.ts, src/vendors/sanmar/types/pricing.ts, src/vendors/sanmar/types/inventory.ts, src/vendors/sanmar/types/media.ts, src/vendors/sanmar/types/index.ts, src/vendors/sanmar/constants.ts, src/vendors/sanmar/auth.ts, src/vendors/sanmar/client.ts</files>
  <action>
Port the following from scripts/sanmar/ to src/vendors/sanmar/:

1. **types/** — Copy all 6 type files (auth.ts, product.ts, pricing.ts, inventory.ts, media.ts, index.ts) as-is. These are pure type definitions with no external dependencies.

2. **constants.ts** — Copy as-is. Contains WSDL URLs, warehouse definitions, pricing codes, etc.

3. **auth.ts** — Port with one key change: remove the `import 'dotenv/config'` line (dotenv is loaded once at startup in src/config.ts, not per-module). Keep loadCredentials(), getSanMarAuth(), getPromoStandardsAuth(), validateCredentials() exactly as-is. They read from process.env which is populated by the time any vendor code runs.

4. **client.ts** — Copy as-is. SOAP client factory with Map-based caching, getClient(), convenience functions (getProductInfoClient, getInventoryClient, etc.), debug helpers, clearClientCache(). Import paths stay internal (./constants.js, no external changes needed).

Also check if scripts/sanmar/utils/ exists with error-handler.ts and retry.ts. If so, port those to src/vendors/sanmar/utils/ as well. Update any import paths that reference '../utils/' accordingly.

All internal imports between these files use relative paths (./types/index.js, ./constants.js, etc.) which remain valid after the port.
  </action>
  <verify>npx tsc --noEmit passes with no errors in src/vendors/sanmar/</verify>
  <done>SanMar types, constants, auth, client, and utils ported to src/vendors/sanmar/. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Port SanMar service functions and public API barrel</name>
  <files>src/vendors/sanmar/services/product.ts, src/vendors/sanmar/services/pricing.ts, src/vendors/sanmar/services/inventory.ts, src/vendors/sanmar/services/media.ts, src/vendors/sanmar/services/index.ts, src/vendors/sanmar/index.ts</files>
  <action>
Port service modules and barrel export:

1. **services/product.ts** — Port from scripts/sanmar/services/product.ts. Contains getProductByStyle, getProductByStyleAndColor, getProductVariant, extractUniqueColors, extractAvailableSizes. Internal imports reference ../client.js, ../auth.js, ../types/index.js — all relative, stay valid.

2. **services/pricing.ts** — Port from scripts/sanmar/services/pricing.ts. Contains getStylePricing, getVariantPricing, isSaleActive, getEffectivePrice, getSuggestedRetail, formatPricingSummary.

3. **services/inventory.ts** — Port from scripts/sanmar/services/inventory.ts. Contains getStyleInventory, getInventoryBatch, getTotalQuantity, isInStock, isWellStocked, getWarehouseBreakdown, formatInventorySummary.

4. **services/media.ts** — Port from scripts/sanmar/services/media.ts. Contains getProductImages, getFrontImages, getSwatchImages, getImagesByColor, groupImagesByColor.

5. **services/index.ts** — Port as-is (barrel re-export of all service modules).

6. **index.ts** — Port from scripts/sanmar/index.ts. This is the public API barrel. Update to NOT export the adapter (that goes in Plan 04). Remove the `SanMarError, SanMarErrorType, isRetryable` exports if the error-handler lives in utils/ (adjust path). Keep all service, type, constant, and auth exports.

Key: Remove `import 'dotenv/config'` from any service files that have it (auth.ts already handled in Task 1). The v2.0 app loads dotenv once at startup.

Do NOT port scripts/sanmar/demo.ts — that's a CLI script, not a library module.
  </action>
  <verify>npx tsc --noEmit passes. All exports from src/vendors/sanmar/index.ts resolve correctly.</verify>
  <done>All SanMar service functions ported. Public API barrel (index.ts) exports all services, types, constants, and auth functions. No demo/CLI code included.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npx tsc --noEmit passes with no errors
- [ ] src/vendors/sanmar/index.ts exports: getProductByStyle, getStylePricing, getStyleInventory, getProductImages, validateCredentials
- [ ] src/vendors/sanmar/client.ts exports: getClient, getProductInfoClient, describeClient, clearClientCache
- [ ] All internal imports resolve (no broken paths)
- [ ] No dotenv/config imports in any src/vendors/sanmar/ file
</verification>

<success_criteria>
- All tasks completed
- Complete SanMar SOAP client library at src/vendors/sanmar/
- TypeScript compiles without errors
- All public API functions accessible via src/vendors/sanmar/index.ts
</success_criteria>

<output>
After completion, create `.planning/phases/46-product-pipeline-vendor-apis/46-02-SUMMARY.md`
</output>
