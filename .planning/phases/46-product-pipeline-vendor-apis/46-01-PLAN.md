---
phase: 46-product-pipeline-vendor-apis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vendors/types.ts
  - src/vendors/registry.ts
  - src/vendors/index.ts
  - src/config.ts
autonomous: true
---

<objective>
Port the vendor abstraction layer from scripts/vendor/ into src/vendors/ and extend config with vendor credential env vars.

Purpose: Establish the unified type system and adapter registry that both vendor clients (SanMar, S&S) depend on. This is the foundation for all vendor API work in the v2.0 backend.
Output: src/vendors/ with VendorAdapter interface, unified types, and registry. Config extended with vendor credential fields.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@scripts/vendor/types.ts
@scripts/vendor/registry.ts
@scripts/vendor/index.ts
@src/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port vendor abstraction layer to src/vendors/</name>
  <files>src/vendors/types.ts, src/vendors/registry.ts, src/vendors/index.ts</files>
  <action>
Port the three files from scripts/vendor/ into src/vendors/:

1. **src/vendors/types.ts** — Copy scripts/vendor/types.ts. Update module-level comments to reference v2.0 architecture. Keep all type definitions (VendorId, UnifiedProduct, UnifiedPricing, UnifiedWarehouse, UnifiedInventory, UnifiedMedia, UnifiedProductData) exactly as-is. These types are the contract consumed by pipeline, monitor, and sync systems.

2. **src/vendors/registry.ts** — Copy scripts/vendor/registry.ts. Update import path for types to './types.js'. Keep VendorAdapter interface, registerVendor, getVendor, getDefaultVendor, listVendors, isVendorRegistered, parseVendorFlag exactly as-is.

3. **src/vendors/index.ts** — Barrel re-export: `export * from './types.js'` and `export * from './registry.js'`.

Key: These files have ZERO dependencies on scripts/ code — they are self-contained type definitions and a Map-based registry. The port is a straight copy with path adjustments.
  </action>
  <verify>npx tsc --noEmit passes with no errors related to src/vendors/</verify>
  <done>src/vendors/types.ts, registry.ts, index.ts exist with all unified types and VendorAdapter interface. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extend config with vendor credential env vars</name>
  <files>src/config.ts</files>
  <action>
Add vendor credential fields to the Config interface and loadConfig():

```
// SanMar credentials (SOAP API)
sanmarCustomerNumber: string | undefined
sanmarUsername: string | undefined
sanmarPassword: string | undefined

// S&S Activewear credentials (REST API)
ssAccountNumber: string | undefined
ssApiKey: string | undefined
```

In loadConfig(), read from process.env:
- SANMAR_CUSTOMER_NUMBER, SANMAR_USERNAME, SANMAR_PASSWORD
- SS_ACCOUNT_NUMBER, SS_API_KEY

All credential fields are `string | undefined` (not required at startup — the app can run without vendor APIs configured, and each vendor validates credentials on first use).

Do NOT add dotenv/config import — it's already imported at the top of config.ts.
  </action>
  <verify>npx tsc --noEmit passes. The Config interface includes all 5 new credential fields.</verify>
  <done>Config interface extended with vendor credential fields. loadConfig() reads from process.env. All fields optional (undefined when not set).</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npx tsc --noEmit passes with no errors
- [ ] src/vendors/types.ts exports VendorId, UnifiedProduct, UnifiedPricing, UnifiedWarehouse, UnifiedInventory, UnifiedMedia, UnifiedProductData
- [ ] src/vendors/registry.ts exports VendorAdapter, registerVendor, getVendor, listVendors, parseVendorFlag
- [ ] src/config.ts Config interface includes sanmarCustomerNumber, sanmarUsername, sanmarPassword, ssAccountNumber, ssApiKey
</verification>

<success_criteria>
- All tasks completed
- TypeScript compiles without errors
- Vendor abstraction layer fully ported to src/vendors/
- Config extended with vendor credential fields
</success_criteria>

<output>
After completion, create `.planning/phases/46-product-pipeline-vendor-apis/46-01-SUMMARY.md`
</output>
