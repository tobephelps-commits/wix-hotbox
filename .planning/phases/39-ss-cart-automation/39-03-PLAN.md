---
phase: 39-ss-cart-automation
plan: 03
type: execute
wave: 3
depends_on: ["39-02"]
files_modified:
  - scripts/orders/ss-cart-cli.ts
  - scripts/pipeline/preview-server.ts
  - scripts/pipeline/preview.html
  - package.json
autonomous: false
---

<objective>
Add CLI command and dashboard UI for S&S cart automation, matching the SanMar pattern.

Purpose: Provides operator interface for S&S cart filling via both command line and browser dashboard.
Output: ss-cart-cli.ts, preview server endpoints, and dashboard UI with visual verification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-sanmar-cart-automation/19-03-SUMMARY.md
@.planning/phases/39-ss-cart-automation/39-02-SUMMARY.md

# Source files — mirror this pattern:
@scripts/orders/cart-cli.ts
@scripts/pipeline/preview-server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create S&S cart fill CLI</name>
  <files>scripts/orders/ss-cart-cli.ts</files>
  <action>
Create ss-cart-cli.ts mirroring cart-cli.ts but for S&S:

1. Import dotenv/config for env loading
2. Import SS consolidator: getSSOrdersForCartFill, consolidateSSOrders
3. Import SS filler: fillSSCartForPendingOrders
4. Import cart types: CartItem, CartFillResult

5. Create ANSI color helpers (copy from cart-cli.ts)

6. Create flag parsing helpers: hasFlag, getFlag, parseStatuses

7. Create cmdPreview(args):
   - Parse --status flag (default: ['new'])
   - Load orders with getSSOrdersForCartFill
   - Consolidate with consolidateSSOrders
   - Display table of consolidated S&S items
   - Show skipped items count
   - Prompt: "Run with --fill to execute cart automation"

8. Create cmdFill(args):
   - Parse --status and --no-headless flags
   - Call fillSSCartForPendingOrders
   - Display results with displayFillResult helper

9. Create displayFillResult(result) — same as SanMar CLI

10. Create cmdHelp() — update for S&S specific options

11. Create CLI router (same pattern as cart-cli.ts)

Key differences from SanMar CLI:
- Header: "S&S Activewear Cart Preview" / "S&S Activewear Cart Fill"
- Functions: getSSOrdersForCartFill, consolidateSSOrders, fillSSCartForPendingOrders
- Log messages reference S&S instead of SanMar
- Help text describes S&S cart commands
  </action>
  <verify>npx tsc --noEmit passes, npx tsx scripts/orders/ss-cart-cli.ts help shows usage</verify>
  <done>ss-cart-cli.ts provides preview, fill, and help commands</done>
</task>

<task type="auto">
  <name>Task 2: Add package.json scripts for S&S cart</name>
  <files>package.json</files>
  <action>
Add npm scripts for S&S cart commands to package.json:

```json
"ss-cart": "tsx scripts/orders/ss-cart-cli.ts",
"ss-cart:preview": "tsx scripts/orders/ss-cart-cli.ts preview",
"ss-cart:fill": "tsx scripts/orders/ss-cart-cli.ts --fill"
```

Place these after the existing SanMar cart scripts to maintain grouping:
- cart / cart:preview / cart:fill (SanMar)
- ss-cart / ss-cart:preview / ss-cart:fill (S&S - new)
  </action>
  <verify>npm run ss-cart help shows usage</verify>
  <done>npm run ss-cart commands available</done>
</task>

<task type="auto">
  <name>Task 3: Add S&S cart preview/fill API endpoints</name>
  <files>scripts/pipeline/preview-server.ts</files>
  <action>
Add API endpoints for S&S cart operations to preview-server.ts:

1. Add imports at top:
   ```typescript
   import { getSSOrdersForCartFill, consolidateSSOrders } from '../orders/ss-cart-consolidator.js';
   import { fillSSCartForPendingOrders, saveSSCartFillResult } from '../orders/ss-cart-filler.js';
   ```

2. Add GET /api/ss-cart/preview endpoint:
   - Load orders with getSSOrdersForCartFill({ statuses: ['new'] })
   - Consolidate with consolidateSSOrders
   - Return JSON: { items, orderNumbers, orderCount, totalItems }
   - Mirror the existing /api/cart/preview pattern

3. Add POST /api/ss-cart/fill endpoint:
   - Call fillSSCartForPendingOrders({ headless: true })
   - Save result with saveSSCartFillResult
   - Return JSON: { status, successCount, failedCount, checkoutUrl, error }
   - Mirror the existing /api/cart/fill pattern

4. Add GET /api/ss-cart/history endpoint (optional but useful):
   - Read files from ./data/cart-fills/ss/
   - Return last 10 fill results sorted by date descending
   - Mirror existing /api/cart/history pattern
  </action>
  <verify>npm run dev, then curl localhost:3456/api/ss-cart/preview returns JSON</verify>
  <done>S&S cart API endpoints operational</done>
</task>

<task type="auto">
  <name>Task 4: Add S&S cart UI to dashboard</name>
  <files>scripts/pipeline/preview.html</files>
  <action>
Add S&S cart fill button and modal to the dashboard:

1. In the Orders section header (around line 5568), add a second cart button:
   ```html
   <button type="button" class="btn-ss-cart-fill" id="fillSSCartBtn" disabled>Fill S&S Cart</button>
   ```
   Place AFTER the existing "Fill SanMar Cart" button.

2. Add CSS for .btn-ss-cart-fill (use different color, e.g., teal #00897b):
   ```css
   .btn-ss-cart-fill {
     background: #00897b;
     color: #fff;
     border: none;
     padding: 4px 12px;
     border-radius: 4px;
     font-size: 12px;
     font-weight: 500;
     cursor: pointer;
     transition: background 0.15s;
     margin-left: 8px;
   }
   .btn-ss-cart-fill:hover { background: #00796b; }
   .btn-ss-cart-fill:disabled { background: #bbb; cursor: not-allowed; }
   ```

3. Add a second cart modal for S&S (copy structure from SanMar modal):
   ```html
   <!-- S&S Cart Fill Preview Modal (Phase 39) -->
   <div class="cart-modal-overlay" id="ssCartModalOverlay">
     <div class="cart-modal">
       <h2>Fill S&S Activewear Cart</h2>
       <div class="cart-summary" id="ssCartSummary"></div>
       <div id="ssCartPreviewContent">
         <div class="cart-loading-msg">Loading cart preview...</div>
       </div>
       <div class="cart-modal-actions" id="ssCartModalActions">
         <button type="button" class="btn btn-outline" id="ssCartCancelBtn">Cancel</button>
         <button type="button" class="btn-ss-cart-fill" id="ssCartExecuteBtn" style="padding:8px 20px;font-size:14px;">Fill Cart</button>
       </div>
     </div>
   </div>
   ```
   Place after the existing SanMar cart modal.

4. Add JavaScript handlers in the Orders section (mirror SanMar pattern):
   - fillSSCartBtn click → open ssCartModalOverlay, fetch /api/ss-cart/preview
   - ssCartCancelBtn click → close modal
   - ssCartExecuteBtn click → POST /api/ss-cart/fill, show results, close modal
   - Enable/disable button based on S&S order count

5. Update loadOrders() to enable/disable S&S button:
   ```javascript
   // Enable/disable Fill S&S Cart button based on new orders with S&S items
   var ssNewCount = _ordersCache.filter(function(o) {
     return o.status === 'new' && o.lineItems.some(function(li) { return li.vendor === 'ss'; });
   }).length;
   var fillSSBtn = document.getElementById('fillSSCartBtn');
   if (fillSSBtn) fillSSBtn.disabled = ssNewCount === 0;
   ```
  </action>
  <verify>npm run dev, open localhost:3456, verify both cart buttons appear in Orders tab</verify>
  <done>S&S cart fill button and modal added to dashboard</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>S&S cart fill UI in dashboard alongside existing SanMar cart fill</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3456
    3. Navigate to Orders tab
    4. Verify: Both "Fill SanMar Cart" (orange) and "Fill S&S Cart" (teal) buttons visible
    5. Click "Fill S&S Cart" button
    6. Verify: Modal opens with "Fill S&S Activewear Cart" header
    7. Modal should show "No S&S items to order" if no S&S orders exist
    8. Close modal with Cancel or X
    9. Verify: No console errors
  </how-to-verify>
  <resume-signal>Type "approved" if both cart buttons work, or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes without errors
- [ ] `npm run ss-cart help` shows usage
- [ ] `curl localhost:3456/api/ss-cart/preview` returns JSON
- [ ] Dashboard shows both SanMar and S&S cart buttons
- [ ] S&S modal opens and displays preview/fill functionality
</verification>

<success_criteria>
- All tasks completed
- TypeScript compiles without errors
- CLI provides preview, fill, and help commands
- Dashboard has S&S cart button and modal
- Visual verification passed
- Phase 39 complete
</success_criteria>

<output>
After completion, create `.planning/phases/39-ss-cart-automation/39-03-SUMMARY.md`
</output>
