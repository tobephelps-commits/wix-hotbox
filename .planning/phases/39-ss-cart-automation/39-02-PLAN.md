---
phase: 39-ss-cart-automation
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - scripts/orders/ss-cart-filler.ts
  - scripts/orders/index.ts
autonomous: false
---

<objective>
Build Playwright browser automation engine for S&S Activewear cart filling with selector discovery.

Purpose: Automates adding consolidated S&S items to ssactivewear.com shopping cart, then hands off visible browser for manual checkout — matching the SanMar cart filler pattern.
Output: ss-cart-filler.ts with fillSSCart, fillSSCartForPendingOrders, saveSSCartFillResult, and markSSOrdersAsOrdered functions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-sanmar-cart-automation/19-02-SUMMARY.md
@.planning/phases/39-ss-cart-automation/39-01-SUMMARY.md

# Source files — mirror this pattern for S&S:
@scripts/orders/sanmar-cart-filler.ts
@scripts/orders/cart-types.ts
@scripts/orders/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover S&S website structure via Playwright</name>
  <files>scripts/orders/ss-cart-filler.ts</files>
  <action>
Create ss-cart-filler.ts with the initial structure and login flow. The selector discovery happens during this task.

1. Create the file with imports and constants:
   - Import Playwright (chromium, Browser, BrowserContext, Page)
   - Import cart types and order functions
   - Define SS_BASE = 'https://www.ssactivewear.com'
   - Define SS_SIGNIN = 'https://www.ssactivewear.com/signin' (verify actual URL)
   - Define SS_CART = 'https://www.ssactivewear.com/cart' (verify actual URL)
   - Define SS_CART_FILLS_DIR = './data/cart-fills/ss'
   - Define DEFAULT_ITEM_TIMEOUT = 60_000
   - Define ACTION_DELAY = 500

2. Create SSCartFillOptions interface (mirror SanMar pattern)

3. Create getSSCredentials() helper:
   - Read SS_WEB_USERNAME and SS_WEB_PASSWORD from env
   - Throw descriptive error if missing

4. Create delay() helper (same as SanMar)

5. Create loginToSS(page: Page) function:
   **DISCOVERY REQUIRED:** Use Playwright headed mode to manually navigate to ssactivewear.com and identify:
   - Sign-in page URL (may differ from /signin)
   - Email/username input selector
   - Password input selector
   - Submit button selector
   - Success indicator (URL change, element appearing)
   - Error message selectors

   Document discovered selectors in code comments for future maintenance.

6. Create addSSItemToCart(page: Page, item: CartItem, timeout: number) function:
   **DISCOVERY REQUIRED:** Navigate to a product page and identify:
   - Product URL pattern: likely https://www.ssactivewear.com/p/{style} or similar
   - Color selection mechanism (swatch? dropdown? buttons?)
   - Size selection mechanism
   - Quantity input field
   - "Add to Cart" button selector
   - Cart confirmation modal/toast
   - Out-of-stock/error message selectors

   Use multi-strategy selectors like SanMar (try swatch first, then dropdown, then button).
   Document all discovered selectors in code comments.

7. Create selectSSColor(page, colorName, timeout) helper with multi-strategy approach
8. Create selectSSSize(page, sizeName, timeout) helper with multi-strategy approach
9. Create setSSQuantity(page, quantity, timeout) helper

The discovery happens by launching Playwright in headed mode (headless: false) and manually observing the DOM. Include defensive fallback selectors where possible.
  </action>
  <verify>npx tsc --noEmit passes for partial file</verify>
  <done>ss-cart-filler.ts has login flow and item addition with discovered selectors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>S&S Activewear login flow and basic item addition selectors</what-built>
  <how-to-verify>
    1. Ensure SS_WEB_USERNAME and SS_WEB_PASSWORD are set in .env
    2. Run a quick test: `npx tsx -e "import { chromium } from 'playwright'; const b = await chromium.launch({headless:false}); const p = await b.newPage(); await p.goto('https://www.ssactivewear.com/signin'); await new Promise(r => setTimeout(r, 30000)); await b.close();"`
    3. Watch browser navigate to S&S sign-in page
    4. Verify the page loads and sign-in form is visible
    5. Close browser after observation

    If sign-in page URL is different, note the correct URL.
    If login form structure differs from expected, describe what you see.
  </how-to-verify>
  <resume-signal>Type "approved" if login page accessible, or describe issues found</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Complete browser automation engine with orchestrator</name>
  <files>scripts/orders/ss-cart-filler.ts</files>
  <action>
Complete ss-cart-filler.ts with the full automation engine:

1. Create fillSSCart(request: CartFillRequest, options?: SSCartFillOptions):
   - Launch browser (headless by default)
   - Login to S&S
   - Iterate through items, call addSSItemToCart for each
   - Track per-item results (success/failure with error messages)
   - On completion: save storageState, close headless browser
   - Relaunch in headed mode at cart page for manual checkout
   - Return CartFillResult with status (success/partial/failed)

2. Create saveSSCartFillResult(result: CartFillResult):
   - Ensure SS_CART_FILLS_DIR exists (./data/cart-fills/ss/)
   - Save to fill-{timestamp}.json
   - Log file path

3. Create markSSOrdersAsOrdered(result: CartFillResult):
   - If status === 'failed', skip updates
   - For each order in result.request.orderNumbers:
     - Only update orders currently in 'new' status
     - Set to 'ordered' with note about S&S cart fill
   - Log each update or skip

4. Create fillSSCartForPendingOrders(options?) orchestrator:
   - Load orders with getSSOrdersForCartFill
   - Consolidate with consolidateSSOrders
   - Call fillSSCart
   - Save result
   - Mark orders as ordered
   - Return result or null if no orders

Mirror the SanMar implementation closely but with SS-specific:
- Constants (URLs, directory paths)
- Credential function (getSSCredentials)
- Log prefix: [ss-cart]
- Selectors (from Task 1 discovery)
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <done>Complete S&S cart filler with all functions implemented</done>
</task>

<task type="auto">
  <name>Task 3: Add barrel exports for S&S cart filler</name>
  <files>scripts/orders/index.ts</files>
  <action>
Add exports for the S&S cart filler to scripts/orders/index.ts:

```typescript
// S&S Activewear cart automation (Phase 39)
export {
  fillSSCart,
  fillSSCartForPendingOrders,
  saveSSCartFillResult,
  markSSOrdersAsOrdered,
} from './ss-cart-filler.js';
```

Place after the S&S consolidator exports.
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <done>S&S cart filler functions exported from orders module barrel</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes without errors
- [ ] ss-cart-filler.ts has all four main functions
- [ ] Login flow works (tested via checkpoint)
- [ ] Selectors documented in code comments
- [ ] Functions exported from scripts/orders/index.ts
</verification>

<success_criteria>
- All tasks completed
- TypeScript compiles without errors
- S&S cart filler mirrors SanMar pattern
- Selectors discovered and documented
- Ready for Plan 39-03 CLI and dashboard integration
</success_criteria>

<output>
After completion, create `.planning/phases/39-ss-cart-automation/39-02-SUMMARY.md`
</output>
