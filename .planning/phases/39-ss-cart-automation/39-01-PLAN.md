---
phase: 39-ss-cart-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/orders/ss-cart-consolidator.ts
  - scripts/orders/index.ts
autonomous: true
user_setup:
  - service: S&S Activewear
    why: "Web login credentials for cart automation (separate from API credentials)"
    env_vars:
      - name: SS_WEB_USERNAME
        source: "S&S Activewear website login email used for ssactivewear.com"
      - name: SS_WEB_PASSWORD
        source: "S&S Activewear website login password"
    account_setup:
      - url: "https://www.ssactivewear.com"
        skip_if: "Already have S&S Activewear web account"
---

<objective>
Create S&S-specific order consolidation engine that filters orders to S&S vendor items only.

Purpose: Provides the data layer for S&S cart automation, mirroring the SanMar consolidator pattern but filtering for vendor='ss' instead of SanMar.
Output: ss-cart-consolidator.ts with getSSOrdersForCartFill and consolidateSSOrders functions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-sanmar-cart-automation/19-01-SUMMARY.md

# Source files to mirror pattern from:
@scripts/orders/cart-types.ts
@scripts/orders/cart-consolidator.ts
@scripts/orders/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create S&S cart consolidator module</name>
  <files>scripts/orders/ss-cart-consolidator.ts</files>
  <action>
Create ss-cart-consolidator.ts mirroring the SanMar cart-consolidator.ts pattern but filtering for S&S vendor items:

1. Import types from cart-types.ts (reuse CartItem, CartFillRequest) and types.ts (Order, OrderStatus)
2. Import listOrders from order-store.ts

3. Create `getSSOrdersForCartFill(filter?: { statuses?: OrderStatus[] })`:
   - Default statuses to ['new']
   - Load orders for each status via listOrders
   - Deduplicate by id
   - Sort by orderNumber ascending
   - Return Order[]

4. Create `consolidateSSOrders(orders: Order[])`:
   - Filter line items where `vendor === 'ss'` (ONLY include S&S items)
   - Skip items where vendor is 'sanmar' or undefined
   - Skip items without vendorStyle, color, or size (log warnings)
   - Group by composite key: `style:color:size` (lowercased for comparison)
   - Sum quantities across matching items
   - Track sourceOrders for audit trail
   - Return CartFillRequest with sorted items

Key differences from SanMar consolidator:
- Filter condition: `vendor === 'ss'` (include) vs `vendor === 'ss'` (exclude) in SanMar
- Console log prefix: `[ss-cart-consolidator]` instead of `[cart-consolidator]`
- Empty cart message: "no items to order from S&S Activewear"

Do NOT modify cart-types.ts — the CartItem and CartFillRequest types work for both vendors.
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <done>ss-cart-consolidator.ts exports getSSOrdersForCartFill and consolidateSSOrders functions</done>
</task>

<task type="auto">
  <name>Task 2: Add barrel exports for S&S consolidator</name>
  <files>scripts/orders/index.ts</files>
  <action>
Add exports for the new S&S consolidator functions to scripts/orders/index.ts:

```typescript
// S&S Activewear cart consolidation (Phase 39)
export { getSSOrdersForCartFill, consolidateSSOrders } from './ss-cart-consolidator.js';
```

Place after the existing SanMar cart exports to maintain logical grouping:
- Cart types
- SanMar cart consolidation
- SanMar cart filler
- S&S cart consolidation (new)
  </action>
  <verify>npx tsc --noEmit passes, exports visible in index.ts</verify>
  <done>S&S consolidator functions exported from orders module barrel</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes without errors
- [ ] ss-cart-consolidator.ts exists with both functions
- [ ] Functions are exported from scripts/orders/index.ts
- [ ] Consolidator correctly filters for vendor='ss' only
</verification>

<success_criteria>
- All tasks completed
- TypeScript compiles without errors
- S&S consolidator mirrors SanMar pattern but filters for S&S vendor
- Functions exported and ready for Plan 39-02 browser automation
</success_criteria>

<output>
After completion, create `.planning/phases/39-ss-cart-automation/39-01-SUMMARY.md`
</output>
