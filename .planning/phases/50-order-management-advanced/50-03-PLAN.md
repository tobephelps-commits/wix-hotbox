---
phase: 50-order-management-advanced
plan: 03
type: execute
wave: 2
depends_on: ["50-02"]
files_modified:
  - ui/src/components/orders/PipelineView.tsx
  - ui/src/components/orders/PipelineView.css
  - ui/src/components/orders/BulkToolbar.tsx
  - ui/src/components/orders/OrdersTab.tsx
  - ui/src/components/orders/OrdersTab.css
  - ui/src/components/orders/OrderList.tsx
autonomous: true
---

<objective>
Add pipeline visualization (kanban-style status board) and bulk order operations.

Purpose: Provide visual order pipeline with stage cards, aging indicators, and attention badges — plus multi-select bulk actions for efficient order management on the touch kiosk.
Output: PipelineView component with kanban stages and BulkToolbar with batch operations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-order-management-advanced/50-02-SUMMARY.md

# v2.0 order types (status transitions, aging thresholds):
@src/orders/types.ts

# Order API routes (summary/extended, bulk/status):
@src/routes/orders.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PipelineView with kanban stages and aging indicators</name>
  <files>ui/src/components/orders/PipelineView.tsx, ui/src/components/orders/PipelineView.css, ui/src/components/orders/OrdersTab.tsx, ui/src/components/orders/OrdersTab.css</files>
  <action>
  **PipelineView.tsx:** Kanban-style pipeline visualization showing orders grouped by status stage.
  - Fetch extended summary from `GET /api/orders/summary/extended` for stage metrics and aging data
  - Fetch all active orders from `GET /api/orders?limit=200` (active statuses only, exclude delivered/cancelled)
  - Display as horizontal scrollable row of stage columns:
    - Columns for: new, ordered, received, in-production, packed, shipped
    - Each column header: status name, order count badge, total value
    - Column cards: each order as a compact card (order number, customer name, item count, total, time-in-status)
  - **Aging indicators:** Orders exceeding AGING_THRESHOLDS get a warning badge. Use thresholds: new>48h, in-production>72h, packed>24h. Show hours in status. Orange for approaching threshold (>75%), red for exceeded.
  - **Attention badges:** At the top of PipelineView, show attention summary from extended summary:
    - Cart fill pending (new orders needing vendor ordering)
    - Stuck in production (>72h)
    - Awaiting shipment (packed >24h)
    - Unresolved errors
    Each as a colored pill/badge. Tap to filter to that status.
  - Clicking an order card calls `onOrderSelect(orderId)` to show detail in the detail panel
  - Touch: horizontal scroll for columns (CSS overflow-x: auto with scroll-snap), vertical scroll within columns

  **OrdersTab.tsx modifications:**
  - Add `viewMode` toggle ('list' | 'pipeline') — two icon buttons in the header area
  - When viewMode === 'pipeline', render `<PipelineView>` instead of `<OrderList>`
  - Both views share the same `onOrderSelect` callback to show OrderDetail
  - Default to 'list' view, persist preference in localStorage

  **PipelineView.css:** Kanban layout styles.
  - `.pipeline-container`: horizontal flex with overflow-x auto, scroll-snap-type: x mandatory
  - `.pipeline-stage`: vertical column, min-width 250px, scroll-snap-align: start, max-height calc(100vh - header)
  - `.pipeline-card`: compact order card with shadow, border-left colored by status, 12px padding
  - `.pipeline-aging`: warning/danger badge styles (orange/red)
  - `.pipeline-attention`: attention summary bar at top with pill badges
  - Touch-optimized: scroll-snap for swipe between stages, large tap targets on cards
  </action>
  <verify>npm run ui:build succeeds; PipelineView renders with stage columns</verify>
  <done>PipelineView shows kanban stages with order cards, aging indicators, and attention badges; view toggle works in OrdersTab</done>
</task>

<task type="auto">
  <name>Task 2: Add bulk selection and batch operations toolbar</name>
  <files>ui/src/components/orders/BulkToolbar.tsx, ui/src/components/orders/OrderList.tsx, ui/src/components/orders/OrdersTab.tsx, ui/src/components/orders/OrdersTab.css</files>
  <action>
  **OrdersTab.tsx modifications:**
  - Add `selectedOrderIds: Set<string>` state for multi-select
  - Pass selection state and toggle handlers to OrderList and BulkToolbar
  - Show BulkToolbar when selectedOrderIds.size > 0 (sticky at bottom of orders panel)

  **OrderList.tsx modifications:**
  - Add checkbox/tap-to-select on each order row for multi-select
  - Long-press or checkbox tap enters selection mode
  - "Select All" / "Deselect All" in header when in selection mode
  - Visual: selected rows get highlighted border/background
  - Selection preserved across filter changes (Phase 41 pattern)

  **BulkToolbar.tsx:** Sticky bottom toolbar appearing when orders are selected.
  - Shows: "{N} orders selected" count
  - Action buttons (touch-friendly, min 44px height):
    1. **Bulk Status Update:** Opens a dropdown/popover with status options. On select, POSTs to `/api/orders/bulk/status` with `{ orderIds, status }`. Shows success/failure count. Refresh list after.
    2. **Batch Production Sheets:** POSTs to `/api/orders/bulk/production-sheets` with `{ orderIds }`. For single order, opens PDF. For multiple, downloads ZIP. Use fetch + blob URL for download.
    3. **Deselect All:** Clears selection
  - Partial failure handling: If bulk status returns failures, show a brief toast/banner with "X succeeded, Y failed" and the error details
  - Style: dark background bar (matching header color), white text, action buttons with rounded borders
  - Position: sticky bottom of the orders panel area, z-index above scroll content
  </action>
  <verify>npm run ui:build succeeds; BulkToolbar appears on selection with working actions</verify>
  <done>Multi-select works in order list; BulkToolbar shows with bulk status, batch production sheets, and deselect actions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run ui:build` succeeds
- [ ] Pipeline view shows kanban stages with order cards
- [ ] Aging indicators appear on orders exceeding thresholds
- [ ] Attention badges show at top of pipeline view
- [ ] View toggle switches between list and pipeline views
- [ ] Multi-select works in list view
- [ ] BulkToolbar appears with bulk status and batch PDF actions
- [ ] Bulk status update calls API and refreshes
- [ ] Batch production sheets triggers download
</verification>

<success_criteria>
- Pipeline visualization shows all active order stages
- Aging and attention indicators work correctly
- Bulk operations functional (status update, production sheets)
- Touch-optimized for kiosk display
- View mode persists in localStorage
</success_criteria>

<output>
After completion, create `.planning/phases/50-order-management-advanced/50-03-SUMMARY.md`
</output>
