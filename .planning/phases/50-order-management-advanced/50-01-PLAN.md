---
phase: 50-order-management-advanced
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/orders/pdf-template.ts
  - src/orders/production-sheet.ts
  - src/orders/invoice-generator.ts
  - src/orders/index.ts
  - src/routes/orders.ts
autonomous: true
---

<objective>
Port PDF document generators and add API routes for production sheets and invoices.

Purpose: Enable order-related PDF generation (production sheets for fulfillment, invoices for customers) in the v2.0 Pi appliance architecture, served via REST API endpoints.
Output: Production sheet and invoice PDF generators with API routes for single and bulk generation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-order-management-core/49-02-SUMMARY.md
@.planning/phases/49-order-management-core/49-04-SUMMARY.md

# v1.x source files to port from:
@scripts/orders/invoice-template.ts
@scripts/orders/production-sheet.ts
@scripts/orders/invoice-generator.ts

# v2.0 order types and service:
@src/orders/types.ts
@src/orders/order-service.ts
@src/orders/index.ts
@src/routes/orders.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port PDF template constants and production sheet generator</name>
  <files>src/orders/pdf-template.ts, src/orders/production-sheet.ts</files>
  <action>
  **pdf-template.ts:** Port brand constants and shared layout helpers from v1.x `scripts/orders/invoice-template.ts`:
  - Export BRAND_NAME, BRAND_COLOR, BRAND_SECONDARY, BRAND_LIGHT, PAGE_MARGIN, FONT_SIZE_* constants
  - LOGO_PATH: resolve relative to dataDir (not __dirname). Accept dataDir parameter or use a `setDataDir()` init function matching the v2.0 pattern used in pipeline and logos modules. Fall back to a reasonable default.
  - Port `drawHeader()` and any shared layout helpers, adapting from v1.x Order type to v2.0 `OrderWithDetails` type (v2.0 uses `customerFirstName`/`customerLastName` instead of `customer.firstName`/`customer.lastName`; `items` instead of `lineItems`)

  **production-sheet.ts:** Port from v1.x `scripts/orders/production-sheet.ts`:
  - `ProductGroup` interface and `groupLineItemsByStyle()` — adapt from v1.x `OrderLineItem` to v2.0 `OrderLineItem` (same field names, just different import path)
  - `generateProductionSheet(order: OrderWithDetails): Promise<Buffer>` — returns PDF buffer. Uses PDFKit. Remove logo registry dependency (v2.0 doesn't need position presets for production sheets).
  - `drawHeader()`, `drawOrderSummary()`, `drawProductSection()`, `drawFooter()` — port with v2.0 type adaptations
  - Remove CLI runner and demo order (not needed in v2.0 — API routes serve this)
  - Do NOT import from v1.x files. Rewrite using v2.0 types from `./types.js` and `./pdf-template.js`
  </action>
  <verify>npx tsc --noEmit passes with no errors</verify>
  <done>pdf-template.ts exports brand constants and shared helpers; production-sheet.ts exports generateProductionSheet() returning Buffer</done>
</task>

<task type="auto">
  <name>Task 2: Port invoice generator and add PDF API routes</name>
  <files>src/orders/invoice-generator.ts, src/orders/index.ts, src/routes/orders.ts</files>
  <action>
  **invoice-generator.ts:** Port from v1.x `scripts/orders/invoice-generator.ts`:
  - `generateInvoice(order: OrderWithDetails): Promise<Buffer>` — returns PDF buffer
  - Port the invoice layout: header with logo/title, customer info section, billing/shipping addresses, line items table with columns (Item, SKU, Qty, Unit Price, Total), subtotals section (subtotal, shipping, tax, discount, total), footer
  - Adapt from v1.x Order type to v2.0 OrderWithDetails (same field mapping as production sheet)
  - Use shared constants from `./pdf-template.js`
  - Remove file save and CLI runner (API routes handle delivery)

  **index.ts:** Add re-exports for `generateProductionSheet` and `generateInvoice`

  **orders.ts routes:** Add 3 new endpoints BEFORE the `/:id` route (to prevent path collisions):

  1. `GET /orders/:id/production-sheet` — Returns PDF buffer with `Content-Type: application/pdf` and `Content-Disposition: attachment; filename="PS-{orderNumber}.pdf"`. Fetches order via `getOrder()`, calls `generateProductionSheet()`, returns buffer with `reply.type('application/pdf').send(buffer)`.

  2. `GET /orders/:id/invoice` — Same pattern, filename `INV-{orderNumber}.pdf`, calls `generateInvoice()`.

  3. `POST /orders/bulk/production-sheets` — Accepts `{ orderIds: string[] }`. Generates production sheets for each order. If single order, return PDF directly. If multiple, use `archiver` to create ZIP of all PDFs, return with `Content-Type: application/zip`. Register this BEFORE /:id routes. Handle partial failures (skip failed orders, include error summary in response headers or continue with successful ones).

  **Route ordering:** Register `/bulk/production-sheets` alongside existing `/bulk/status` (before /:id routes). Register `/:id/production-sheet` and `/:id/invoice` as sub-routes of the /:id pattern area.
  </action>
  <verify>npx tsc --noEmit passes; verify route registration order is correct (bulk before parameterized)</verify>
  <done>Invoice generator exports generateInvoice(); 3 PDF endpoints registered; barrel exports updated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run build` succeeds
- [ ] pdf-template.ts, production-sheet.ts, invoice-generator.ts exist in src/orders/
- [ ] index.ts re-exports PDF generators
- [ ] 3 new routes registered in orders.ts (production-sheet, invoice, bulk/production-sheets)
- [ ] Bulk route registered before /:id routes
</verification>

<success_criteria>
- All PDF generators ported with v2.0 type adaptations
- API routes serve PDFs with proper Content-Type headers
- Bulk production sheet endpoint supports ZIP for multiple orders
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/50-order-management-advanced/50-01-SUMMARY.md`
</output>
