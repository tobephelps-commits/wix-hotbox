---
phase: 36-product-migration-tooling
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified: [scripts/pipeline/preview.html]
autonomous: false
---

<objective>
Build the Migration Tooling UI section in the preview dashboard.

Purpose: Provide a point-and-click interface to browse WIX products and identify which need migration through the wizard.
Output: New "Migration" section in preview.html with product browser, filtering, and wizard launch capability.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-product-migration-tooling/36-CONTEXT.md
@.planning/phases/36-product-migration-tooling/36-01-SUMMARY.md

# Key source files:
@scripts/pipeline/preview.html (existing UI patterns, wizard integration)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Migration section CSS styles</name>
  <files>scripts/pipeline/preview.html</files>
  <action>
Add CSS styles for the Migration section, following the established dark theme pattern from Operations Dashboard:

1. Section container styles (similar to operations-section dark theme):
   - Background: linear-gradient similar to wizard-section (#0d1117 to #161b22)
   - Border-radius, padding consistent with other sections

2. Product grid/list styles:
   - Card-based layout for each WIX product
   - Status indicators: tracked (green badge), untracked (orange badge), unknown vendor (gray badge)
   - Hover state with subtle highlight

3. Filter controls:
   - Filter dropdown (All / Untracked / Tracked)
   - Search input for style number or product name
   - Stats display (X total, Y untracked, Z tracked)

4. Product card styles:
   - Product name (bold)
   - Style number, vendor badge
   - Variant count
   - "Migrate" button (for untracked products)
   - "Already Tracked" indicator (for tracked products)

Place styles in the <style> section, grouped with a comment "/* Product Migration (Phase 36) */".
  </action>
  <verify>CSS compiles without syntax errors; styles are defined in preview.html</verify>
  <done>Migration section CSS styles added to preview.html</done>
</task>

<task type="auto">
  <name>Task 2: Add Migration section HTML structure</name>
  <files>scripts/pipeline/preview.html</files>
  <action>
Add HTML structure for Migration section, placed after the Operations section and before Product Info:

```html
<!-- Product Migration (Phase 36) -->
<div class="section migration-section" id="migrationSection">
  <div class="section-header">
    <span>&#128260; PRODUCT MIGRATION</span>
    <div class="migration-controls">
      <input type="text" id="migrationSearch" placeholder="Search style or name..." class="migration-search">
      <select id="migrationFilter">
        <option value="all">All Products</option>
        <option value="untracked" selected>Untracked Only</option>
        <option value="tracked">Tracked Only</option>
      </select>
      <button class="btn btn-sm btn-outline" onclick="loadMigrationProducts()">&#128260; Refresh</button>
    </div>
  </div>
  <div class="migration-stats" id="migrationStats">
    <!-- Populated by JS: X total, Y untracked, Z tracked -->
  </div>
  <div class="migration-grid" id="migrationGrid">
    <!-- Product cards populated by JS -->
    <div class="migration-loading">Loading WIX products...</div>
  </div>
</div>
```

Product card template (rendered by JS):
- Product name as title
- Style number (if detected)
- Vendor badge (SanMar/S&S/Unknown)
- Tracking status badge
- Variant count
- "Migrate" button that opens wizard with pre-filled data
  </action>
  <verify>HTML structure appears in preview.html; no syntax errors</verify>
  <done>Migration section HTML structure added to preview.html</done>
</task>

<task type="auto">
  <name>Task 3: Add Migration section JavaScript</name>
  <files>scripts/pipeline/preview.html</files>
  <action>
Add JavaScript for Migration section functionality:

1. loadMigrationProducts() function:
   - Fetch from /api/wix/products with filter query param
   - Render stats (total, tracked, untracked)
   - Render product cards into #migrationGrid
   - Handle loading and error states

2. renderMigrationCard(product) function:
   - Return HTML string for a single product card
   - Show: name, style (or "Unknown"), vendor badge, tracking badge
   - For untracked products: "Migrate" button
   - For tracked products: "✓ Tracked" indicator (non-clickable)

3. migrateProduct(wixId, style, vendor) function:
   - If style is unknown/null, show alert prompting manual entry
   - If vendor is unknown, default to 'sanmar' with a note
   - Open wizard section (wizardSection.style.display = 'block')
   - Pre-fill wizard vendor select and style input
   - Optionally auto-click fetch to load product data

4. Search/filter functionality:
   - migrationSearch input filters client-side by name or style
   - migrationFilter select triggers fresh API call with ?filter=value

5. Auto-load on page load:
   - Call loadMigrationProducts() after other initializations
   - Default filter to "untracked" to show actionable items first

Place JavaScript in a new section with comment "/* Product Migration (Phase 36) */".
  </action>
  <verify>JavaScript functions defined; no console errors on page load</verify>
  <done>Migration section JavaScript functionality implemented</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Product Migration UI section with WIX product browser and wizard integration</what-built>
  <how-to-verify>
    1. Run: npm run preview
    2. Visit: http://localhost:3456
    3. Verify: Migration section appears below Operations
    4. Check: Products load with tracking status badges
    5. Test: Filter dropdown (All/Untracked/Tracked) filters correctly
    6. Test: Search box filters by style or name
    7. Test: Click "Migrate" on an untracked product → wizard opens with style pre-filled
    8. Verify: Tracked products show "✓ Tracked" instead of Migrate button
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Migration section renders in preview dashboard
- [ ] Products load from /api/wix/products
- [ ] Filter and search work correctly
- [ ] Migrate button opens wizard with pre-filled style/vendor
- [ ] Human verification approved
</verification>

<success_criteria>
- All tasks completed
- Migration UI shows WIX products with tracking status
- Users can filter to untracked products
- Clicking Migrate opens wizard with product pre-loaded
- Human verification passed
</success_criteria>

<output>
After completion, create `.planning/phases/36-product-migration-tooling/36-02-SUMMARY.md`
</output>
