---
phase: 37-dashboard-tabbed-navigation
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified: [scripts/pipeline/preview.html]
autonomous: false
---

<objective>
Move existing dashboard sections into their appropriate tab panels and verify the complete tabbed layout.

Purpose: Complete the dashboard restructuring by relocating all existing sections into the tabbed navigation system.
Output: Fully functional tabbed dashboard with all features accessible through sidebar navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-dashboard-tabbed-navigation/37-01-SUMMARY.md

@scripts/pipeline/preview.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move Products-related sections to Products tab</name>
  <files>scripts/pipeline/preview.html</files>
  <action>
Move the following sections into the #productsPanel tab panel (replace placeholder):

**Sections to move (in this order):**
1. `#batchSection` - Batch Create (Phase 27)
2. `#wizardSection` - Product Pipeline Wizard (Phase 35)
3. `#migrationSection` - Product Migration (Phase 36)
4. `.product-info` - Product info bar (shows after fetch)
5. `#colorsSection` - Colors selection
6. `#sizesSection` - Sizes selection
7. `#logoLibrarySection` - Logo Library (Phase 24)
8. `#logoOverlaySection` - Logo Overlay per-angle (Phase 22)
9. Margin Dashboard section (unlabeled but has "PRODUCT MARGINS" title)
10. `#salesSection` - Sales & Promotions (Phase 15)

**Cut these entire HTML blocks and paste them inside #productsPanel.**

Remove the `.container` wrapper if it was wrapping these - let them flow directly in the tab panel with appropriate padding from the panel itself.

Also move `#initialPrompt` into the Products tab (shown before first fetch).
  </action>
  <verify>No HTML syntax errors, sections appear in Products tab panel</verify>
  <done>All product-related sections relocated to Products tab</done>
</task>

<task type="auto">
  <name>Task 2: Move Orders section to Orders tab</name>
  <files>scripts/pipeline/preview.html</files>
  <action>
Move the following into the #ordersPanel tab panel (replace placeholder):

**Section to move:**
1. `#ordersSection` - Order Dashboard (Phase 18)

The ordersSection contains both the order list view and order detail view.

Keep the Manual Order Form Modal (`#manualOrderModal`) at document level - it's a modal that overlays the entire page.

Keep the Cart Fill Preview Modal (`#cartPreviewModal`) at document level as well.

**Wire up badge:**
In the existing `loadOrders()` function, add a call to update the nav badge:
```javascript
// After orders are loaded and counted
const newOrderCount = orders.filter(o => o.status === 'new').length;
const badge = document.getElementById('ordersNavBadge');
if (badge) {
  badge.textContent = newOrderCount || '';
  badge.style.display = newOrderCount > 0 ? 'inline-block' : 'none';
}
```
  </action>
  <verify>Orders tab shows order management UI, badge shows new order count</verify>
  <done>Orders section in Orders tab, badge updates with new order count</done>
</task>

<task type="auto">
  <name>Task 3: Move Inventory section to Inventory tab</name>
  <files>scripts/pipeline/preview.html</files>
  <action>
Move the following into the #inventoryPanel tab panel (replace placeholder):

**Section to move:**
1. `#inventorySection` - Inventory Dashboard (Phase 16)
   - This includes the `#auditSection` nested inside for the mapping audit

**Wire up badge:**
In the existing `loadTrackedProducts()` or inventory loading function, add badge update:
```javascript
// After products with alerts are counted
const alertCount = products.filter(p => p.hasAlert || p.alertCount > 0).length;
const badge = document.getElementById('inventoryNavBadge');
if (badge) {
  badge.textContent = alertCount || '';
  badge.style.display = alertCount > 0 ? 'inline-block' : 'none';
}
```

The exact property name for alerts may differ - check the actual inventory data structure.
  </action>
  <verify>Inventory tab shows tracked products and audit section, badge shows alert count</verify>
  <done>Inventory section in Inventory tab, badge updates with alert products</done>
</task>

<task type="auto">
  <name>Task 4: Move Customer sections to Customers tab</name>
  <files>scripts/pipeline/preview.html</files>
  <action>
Move the following into the #customersPanel tab panel (replace placeholder):

**Sections to move (in this order):**
1. `#customersSection` - Customer Management (Phase 25)
2. `#royaltySection` - Royalty Reports (Phase 26)

Keep the Customer Form Modal (`#customerFormModal`) at document level.
  </action>
  <verify>Customers tab shows customer list and royalty reporting</verify>
  <done>Customer and royalty sections in Customers tab</done>
</task>

<task type="auto">
  <name>Task 5: Clean up layout and fix any spacing issues</name>
  <files>scripts/pipeline/preview.html</files>
  <action>
Review and fix any layout issues after the move:

1. **Tab panel padding:** Ensure each tab panel has consistent padding (24px) so content isn't cramped against edges
2. **Section spacing:** Maintain gaps between sections within each tab (margin-bottom on sections)
3. **Summary bar / Status bar:** These should stay at the bottom of main-content area, outside tab panels, visible regardless of active tab
4. **Toast notification:** Keep at document level
5. **Remove old container wrapper** if it's now empty or redundant

Add CSS if needed:
```css
.tab-panel {
  padding: 24px;
  display: none;
}
.tab-panel.active {
  display: block;
}
.tab-panel > .section,
.tab-panel > [class*="-section"] {
  margin-bottom: 24px;
}
```
  </action>
  <verify>Consistent spacing, no overlapping elements, summary/status bars visible</verify>
  <done>Layout polished with consistent spacing and proper element positioning</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete tabbed dashboard with sidebar navigation</what-built>
  <how-to-verify>
    1. Run: npm run dev (or however preview server starts - likely node/tsx scripts/pipeline/preview-server.ts)
    2. Visit: http://localhost:3456
    3. Verify sidebar appears on left with 4 nav items (Products, Orders, Inventory, Customers)
    4. Click each nav item - content should switch instantly
    5. Operations Dashboard visible at top regardless of tab
    6. Products tab: Wizard button works, batch create visible, migration browser shows products
    7. Orders tab: Order list loads, order detail view works when clicking an order
    8. Inventory tab: Tracked products show, daemon status visible
    9. Customers tab: Customer list shows, royalty reporting accessible
    10. Refresh page - last active tab should restore
    11. Nav badges: Orders badge shows count if new orders exist
    12. No horizontal scroll, no broken layouts on reasonable screen width (1024px+)
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All sections accessible through appropriate tabs
- [ ] Tab switching instant (no page reload)
- [ ] Operations Dashboard always visible at top
- [ ] Nav badges update with counts
- [ ] All existing functionality preserved (wizard, batch, orders, inventory, customers, royalty)
- [ ] Modals still work (customer form, manual order, cart preview)
- [ ] No console errors
- [ ] Human verification approved
</verification>

<success_criteria>
- Complete tabbed navigation working
- All existing features accessible through new tab structure
- Clean, professional appearance matching dark theme aesthetic
- User can quickly switch between Products, Orders, Inventory, Customers
- Operations Dashboard provides at-a-glance status regardless of active tab
</success_criteria>

<output>
After completion, create `.planning/phases/37-dashboard-tabbed-navigation/37-02-SUMMARY.md`
</output>
