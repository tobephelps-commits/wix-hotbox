---
phase: 37-dashboard-tabbed-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/pipeline/preview.html]
autonomous: true
---

<objective>
Create the sidebar navigation shell and tab switching infrastructure for the dashboard redesign.

Purpose: Establish the foundational layout structure (sidebar + content area) and JavaScript tab switching logic that all subsequent work builds upon.
Output: Working sidebar navigation that can show/hide content areas, with placeholder tab content.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-dashboard-tabbed-navigation/37-CONTEXT.md

@scripts/pipeline/preview.html (lines 1-500 for CSS patterns, lines 4719-4765 for Operations Dashboard pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sidebar navigation CSS</name>
  <files>scripts/pipeline/preview.html</files>
  <action>
Add CSS styles for the sidebar navigation layout. Place after existing CSS around line 1800 (after Operations Dashboard styles).

**Layout structure:**
- `.app-wrapper` - Flex container (row) for sidebar + main content
- `.sidebar` - Fixed-width (200px) dark themed vertical nav bar
- `.sidebar-nav` - Flex column list of nav items
- `.nav-item` - Individual clickable nav item with icon + label
- `.nav-item.active` - Highlighted state for current tab
- `.nav-badge` - Small counter badge for attention indicators
- `.main-content` - Flex-grow content area that holds all tab panels
- `.tab-panel` - Individual tab content containers, hidden by default
- `.tab-panel.active` - Visible tab panel

**Style guidelines:**
- Sidebar: Dark background matching header (#1a1a2e), slim design (200px)
- Nav items: Icon + short label, padding 12px 16px, hover highlight (#2a2a3e)
- Active state: Left border accent (#6c63ff), slightly lighter background
- Badges: Small pill shape, red/orange for counts, positioned right of label
- Content area: Background #f5f5f5 (existing body), padding maintained
- Responsive: Sidebar collapses to icons only below 768px (optional, can defer)

Keep Operations Dashboard section outside tab system (stays at top as global overview).
  </action>
  <verify>CSS parses without errors (no syntax issues visible in browser console)</verify>
  <done>Sidebar and tab panel CSS classes defined with dark theme styling</done>
</task>

<task type="auto">
  <name>Task 2: Add HTML structure for sidebar and tab panels</name>
  <files>scripts/pipeline/preview.html</files>
  <action>
Restructure the HTML after the header to use the new layout. The goal is to wrap existing content in the new structure while keeping sections functional.

**Structure to create:**
```html
<!-- After header -->
<div class="app-wrapper">
  <aside class="sidebar">
    <nav class="sidebar-nav">
      <a href="#" class="nav-item active" data-tab="products">
        <span class="nav-icon">📦</span>
        <span class="nav-label">Products</span>
      </a>
      <a href="#" class="nav-item" data-tab="orders">
        <span class="nav-icon">📋</span>
        <span class="nav-label">Orders</span>
        <span class="nav-badge" id="ordersNavBadge"></span>
      </a>
      <a href="#" class="nav-item" data-tab="inventory">
        <span class="nav-icon">📊</span>
        <span class="nav-label">Inventory</span>
        <span class="nav-badge" id="inventoryNavBadge"></span>
      </a>
      <a href="#" class="nav-item" data-tab="customers">
        <span class="nav-icon">👥</span>
        <span class="nav-label">Customers</span>
      </a>
    </nav>
  </aside>

  <main class="main-content">
    <!-- Operations Dashboard stays outside tabs (global overview) -->
    <div class="operations-section" id="operationsSection">
      ... existing operations content ...
    </div>

    <!-- Tab panels -->
    <div class="tab-panel active" id="productsPanel">
      <!-- Will contain product-related sections in Plan 02 -->
      <div class="tab-placeholder">Products content will be moved here</div>
    </div>

    <div class="tab-panel" id="ordersPanel">
      <div class="tab-placeholder">Orders content will be moved here</div>
    </div>

    <div class="tab-panel" id="inventoryPanel">
      <div class="tab-placeholder">Inventory content will be moved here</div>
    </div>

    <div class="tab-panel" id="customersPanel">
      <div class="tab-placeholder">Customers content will be moved here</div>
    </div>
  </main>
</div>
```

**Important:**
- Keep existing .container wrapper INSIDE the tab panels or remove it (content fills main-content area)
- Operations section stays visible at top of main-content (not in any tab)
- Use placeholder divs for tab content for now - actual sections moved in Plan 02
- Modals (manualOrderModal, customerModal, cartPreviewModal) stay outside the tab structure at document level
- Summary bar and status bar stay at bottom outside tab structure
  </action>
  <verify>HTML is well-formed, page loads without errors</verify>
  <done>Sidebar nav with 4 tabs visible, Operations section at top of content area</done>
</task>

<task type="auto">
  <name>Task 3: Add tab switching JavaScript</name>
  <files>scripts/pipeline/preview.html</files>
  <action>
Add JavaScript for tab navigation. Place in the script section after DOMContentLoaded setup.

**Functionality:**
```javascript
// Tab navigation
function switchTab(tabId) {
  // Update nav items
  document.querySelectorAll('.nav-item').forEach(item => {
    item.classList.toggle('active', item.dataset.tab === tabId);
  });

  // Update tab panels
  document.querySelectorAll('.tab-panel').forEach(panel => {
    panel.classList.toggle('active', panel.id === tabId + 'Panel');
  });

  // Save preference
  localStorage.setItem('activeTab', tabId);
}

// Initialize tab navigation
document.querySelectorAll('.nav-item').forEach(item => {
  item.addEventListener('click', (e) => {
    e.preventDefault();
    switchTab(item.dataset.tab);
  });
});

// Restore last active tab on page load
const savedTab = localStorage.getItem('activeTab');
if (savedTab) {
  switchTab(savedTab);
}
```

**Badge updates:**
Add badge count updates to existing load functions:
- `updateOrdersNavBadge()` - Called from loadOrders(), shows count of 'new' status orders
- `updateInventoryNavBadge()` - Called from loadInventory(), shows count of products with alerts

These can be simple functions that set textContent on badge elements and toggle visibility.
  </action>
  <verify>Click nav items to switch tabs - content area changes, active state updates</verify>
  <done>Tab switching works, state persists across page reload</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Page loads without JavaScript errors
- [ ] Sidebar visible on left with 4 nav items
- [ ] Clicking nav items switches active state
- [ ] Tab panels show/hide correctly
- [ ] Operations Dashboard visible at top (not affected by tab switching)
- [ ] Active tab persists after page reload
</verification>

<success_criteria>
- Sidebar navigation shell is functional
- Tab switching logic works correctly
- Existing Operations Dashboard still visible and functional
- Page does not break or lose existing functionality
- Foundation ready for Plan 02 to move sections into tabs
</success_criteria>

<output>
After completion, create `.planning/phases/37-dashboard-tabbed-navigation/37-01-SUMMARY.md`
</output>
