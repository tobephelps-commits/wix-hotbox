---
phase: 64-integration-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/notifications/trigger.ts
  - src/notifications/email-sender.ts
  - tests/ui/orders.test.ts
autonomous: true
---

<objective>
Polish v2.1 code quality and add UI test coverage for new order creation features.

Purpose: Fix minor type safety issues in notification trigger, improve error logging, and add Playwright UI tests for the ProductPicker + production notes flow.
Output: Cleaner notification code, UI tests for order creation with v2.1 features.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/notifications/trigger.ts
@src/notifications/email-sender.ts
@tests/ui/orders.test.ts
@ui/src/components/orders/OrderCreateForm.tsx
@ui/src/components/orders/ProductPicker.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix notification code polish issues</name>
  <files>src/notifications/trigger.ts, src/notifications/email-sender.ts</files>
  <action>
1. In trigger.ts line 178: `parseInt(orderId, 10) || 0` is unsafe — orderId comes as string from route params. Replace the fallback `|| 0` with proper validation: parse and validate orderId is a valid number at the start of the function, return error result early if not. Same on line 186.

2. In trigger.ts line 90-92: The silent catch on WIX contact lookup swallows errors. Add a console.warn log so operators can see if WIX contact lookups are failing:
   ```
   console.warn(`[Notifications] WIX contact lookup failed for ${email}: ${err instanceof Error ? err.message : String(err)}`);
   ```

3. In email-sender.ts: Verify that the `escapeHtml()` function is applied to ALL user-supplied context fields (customerName, orderNumber, storeName) before insertion into HTML email body. If any field is unescaped, add escapeHtml() calls. This prevents XSS if email clients render HTML.

Keep changes minimal — only fix the identified issues, don't refactor surrounding code.
  </action>
  <verify>cd src && npx tsc --noEmit</verify>
  <done>orderId properly validated, WIX contact errors logged, HTML fields escaped</done>
</task>

<task type="auto">
  <name>Task 2: UI tests for order creation with v2.1 features</name>
  <files>tests/ui/orders.test.ts</files>
  <action>
Add new test cases to the existing tests/ui/orders.test.ts file for v2.1 order creation features:

1. **Production notes in order creation:** Test that the OrderCreateForm shows a "Production Notes" textarea per line item. Navigate to order creation, add an item, verify the notes textarea exists and accepts input.

2. **Production notes display in order detail:** Create an order (via API seed or form submission), navigate to order detail view, verify production notes appear in the item details section.

3. **Browse Catalog button:** Verify the "Browse Catalog" button exists in the order creation form and opens the ProductPicker modal.

Follow existing Playwright UI test patterns in the file. Tests should be self-contained and not depend on external APIs (SanMar/S&S).
  </action>
  <verify>cd src && npx playwright test tests/ui/orders.test.ts --reporter=line</verify>
  <done>UI tests cover production notes and product picker button presence</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes (no type errors)
- [ ] `npx playwright test tests/ui/orders.test.ts` passes
- [ ] Notification trigger handles invalid orderId gracefully
- [ ] Email HTML properly escapes all user-supplied fields
</verification>

<success_criteria>

- Type safety issues in trigger.ts resolved
- HTML escaping complete in email sender
- UI tests verify v2.1 order creation features
- No regressions
  </success_criteria>

<output>
After completion, create `.planning/phases/64-integration-polish/64-02-SUMMARY.md`
</output>
