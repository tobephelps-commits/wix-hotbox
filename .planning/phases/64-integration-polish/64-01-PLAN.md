---
phase: 64-integration-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/api/wix-contacts.test.ts
  - tests/api/notifications.test.ts
  - tests/helpers/db.ts
autonomous: true
---

<objective>
Add API integration tests for WIX Customer Sync and Notification System endpoints.

Purpose: v2.1 added two major API surface areas (wix-contacts, notifications) with zero test coverage. Existing test patterns (Playwright + seeded fixtures) make adding coverage straightforward.
Output: Two new test files covering all v2.1 API routes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tests/fixtures.ts
@tests/helpers/db.ts
@tests/helpers/api.ts
@tests/helpers/server.ts
@src/routes/wix-contacts.ts
@src/routes/notifications.ts
@src/notifications/types.ts
@src/wix-contacts/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed helpers and WIX contacts API tests</name>
  <files>tests/helpers/db.ts, tests/api/wix-contacts.test.ts</files>
  <action>
1. Add `seedTestWixContacts(dataDir)` and `seedTestNotifications(dataDir)` functions to tests/helpers/db.ts following the existing seedTestOrders/seedTestCustomers pattern. WIX contacts seed should insert 2-3 contacts into wix_contacts table (one linked to a customer, one unlinked). Notification seed should insert 2-3 templates into notification_templates (email+sms for 'ordered' stage) and 1-2 log entries into notification_log.

2. Create tests/api/wix-contacts.test.ts following existing test patterns (import from fixtures, use seededApi). Cover:
   - GET /api/wix-contacts — returns list with contacts array
   - GET /api/wix-contacts?search=... — filters by name/email
   - GET /api/wix-contacts?linked=true — filters linked contacts
   - GET /api/wix-contacts/stats — returns stat object with totalContacts, linkedCount, etc.
   - GET /api/wix-contacts/:id — returns single contact, 404 for missing
   - POST /api/wix-contacts/:id/link — links to customer, 400 for invalid customer
   - DELETE /api/wix-contacts/:id/link — unlinks contact

Note: Skip sync endpoints (POST /sync/once, /sync/start, /sync/stop, /sync/health) as they require live WIX API credentials.

Call the seed function from seededServer fixture in fixtures.ts (add seedTestWixContacts and seedTestNotifications alongside existing seeds).
  </action>
  <verify>cd src && npx playwright test tests/api/wix-contacts.test.ts --reporter=line</verify>
  <done>All WIX contacts API tests pass, seeded data works correctly</done>
</task>

<task type="auto">
  <name>Task 2: Notification API tests</name>
  <files>tests/api/notifications.test.ts</files>
  <action>
Create tests/api/notifications.test.ts covering:
   - GET /api/notifications/templates — returns templates array
   - GET /api/notifications/templates/:id — single template, 404 for missing
   - POST /api/notifications/templates — creates template (validate stage enum, channel enum), 400 for invalid stage/channel, 409 for duplicate stage+channel
   - PATCH /api/notifications/templates/:id — updates template fields
   - DELETE /api/notifications/templates/:id — deletes template, 404 for missing
   - GET /api/notifications/log — returns log entries with pagination
   - GET /api/notifications/log/order/:orderId — returns order-specific log entries
   - POST /api/notifications/test — sends test notification (will fail without SMTP config, but should return structured error not 500)

Use seeded notification data from Task 1's seed helpers.
  </action>
  <verify>cd src && npx playwright test tests/api/notifications.test.ts --reporter=line</verify>
  <done>All notification API tests pass</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx playwright test tests/api/wix-contacts.test.ts` passes
- [ ] `npx playwright test tests/api/notifications.test.ts` passes
- [ ] `npx playwright test tests/api/` passes (no regressions in existing tests)
- [ ] No TypeScript errors in test files
</verification>

<success_criteria>

- All v2.1 API routes have test coverage
- Seed helpers follow existing patterns
- No regressions in existing test suite
- Tests verify both success and error paths
  </success_criteria>

<output>
After completion, create `.planning/phases/64-integration-polish/64-01-SUMMARY.md`
</output>
