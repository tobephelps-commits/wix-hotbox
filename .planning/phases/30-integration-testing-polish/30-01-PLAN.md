---
phase: 30-integration-testing-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/smoke-test.ts]
autonomous: true
---

<objective>
Update the smoke test script to validate all v1.0 features: customer accounts, royalty reporting, logo upload/management, multi-angle overlays, batch processing, pipeline preferences, order error tracking, and inventory audit endpoints.

Purpose: The current smoke-test.ts was written for v0.2 (Phase 20). It tests 13 API endpoints and 7 module imports. v1.0 added 15+ new API endpoints and the customers module. This plan upgrades the smoke test to be the final integration gate for v1.0.
Output: Updated smoke-test.ts covering all v1.0 API endpoints and modules.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@scripts/smoke-test.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand smoke test with v1.0 API endpoints and customer module</name>
  <files>scripts/smoke-test.ts</files>
  <action>
Update scripts/smoke-test.ts to cover all v1.0 features:

1. **Update banner** — Change "HotBox v0.2 Smoke Test" to "HotBox v1.0 Smoke Test" and update the file header comment from "v0.2" to "v1.0".

2. **Add new API endpoint tests to ENDPOINT_TESTS array** — these are all GET endpoints that return JSON and don't need parameters:
   - `/api/customers` — Customer list (JSON array)
   - `/api/preferences` — Pipeline preferences (JSON object)
   - `/api/orders/summary` — Order status counts (JSON object)
   - `/api/orders/errors` — Orders with errors (JSON array)

3. **Add customer module import test** — Add to MODULE_TESTS array:
   - No barrel export exists for customers. Instead, add a test that imports the 3 main files individually. Create a single entry: `{ name: 'customers (types+store+royalty)', path: 'customers' }` and for its test, write a temp script that imports `./customers/types.js`, `./customers/store.js`, and `./customers/royalty.js`. Alternatively, the simplest approach is to create a temp file that imports all three and check it runs.

   The existing module import test pattern writes a temp .ts file that does `import '${importPath}'` and checks it runs. Since customers has no index.ts, use a different approach: write a temp script that imports all three customer modules:
   ```
   import './customers/types.js';
   import './customers/store.js';
   import './customers/royalty.js';
   console.log('OK');
   ```

4. **Adjust section counter** — The current output says "[1/4]", "[2/4]", etc. Keep it at 4 sections, just with more tests in sections 3 and 4.

Important implementation notes:
- The new endpoints are simple GET routes that return JSON with no required parameters
- `/api/customers` returns `[]` when no customers exist — that's valid JSON, should pass
- `/api/preferences` returns `{}` or saved preferences object — valid JSON
- `/api/orders/summary` returns counts object — valid JSON
- `/api/orders/errors` returns array — valid JSON
- Do NOT add endpoints that require path parameters (like /api/customers/:id) — those need specific IDs and would 404
- Do NOT add POST endpoints to the test — they have side effects
- Keep the existing tests unchanged, just add the new ones
  </action>
  <verify>npx tsc --noEmit passes (no type errors in updated smoke-test.ts)</verify>
  <done>smoke-test.ts tests 17+ API endpoints (was 13), 8 module groups (was 7), and reports v1.0</done>
</task>

<task type="auto">
  <name>Task 2: Run updated smoke test and fix any issues</name>
  <files>scripts/smoke-test.ts</files>
  <action>
Run `npx tsx scripts/smoke-test.ts` and analyze the results.

For any failures:
- If a new endpoint returns 500: check the handler in preview-server.ts for bugs
- If the customer module import fails: check the import paths match the actual file structure
- If an existing test regresses: investigate and fix

After fixing, re-run the smoke test to confirm all fixes work.

Expected results:
- TypeScript check: PASS
- CLI commands: same as before (some WARN for env-dependent commands)
- New API endpoints: PASS (all return valid JSON even with empty data)
- Customer module import: PASS
- All existing tests: unchanged

Do NOT fix issues that require external services — only fix structural issues.
  </action>
  <verify>npx tsx scripts/smoke-test.ts shows all structural tests passing (17+ endpoints, 8 module groups)</verify>
  <done>All smoke tests pass with v1.0 coverage; any structural issues found are fixed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] smoke-test.ts header says "v1.0"
- [ ] ENDPOINT_TESTS includes /api/customers, /api/preferences, /api/orders/summary, /api/orders/errors
- [ ] MODULE_TESTS includes customers module imports
- [ ] `npm run smoke-test` runs to completion
- [ ] All new endpoint tests pass
- [ ] Customer module import test passes
- [ ] No regressions in existing tests
</verification>

<success_criteria>

- Smoke test validates 17+ API endpoints (4 new v1.0 endpoints added)
- Smoke test validates 8 module groups (customers added)
- Script runs to completion with clear pass/fail reporting
- All structural issues fixed
- Reusable for future regression testing
  </success_criteria>

<output>
After completion, create `.planning/phases/30-integration-testing-polish/30-01-SUMMARY.md`
</output>
