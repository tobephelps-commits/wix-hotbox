---
phase: 38-production-sheets
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - scripts/pipeline/preview.html
autonomous: false
---

<objective>
Add production sheet buttons to dashboard and verify PDF generation.

Purpose: Enable operators to generate production sheets from the Orders tab with single-order and bulk actions.
Output: Dashboard UI with "Production Sheet" button in order detail, visual verification of generated PDF.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-production-sheets/38-CONTEXT.md
@.planning/phases/38-production-sheets/38-01-SUMMARY.md

# Dashboard patterns:
@scripts/pipeline/preview.html (order detail view, print action buttons)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Production Sheet button to order detail view</name>
  <files>scripts/pipeline/preview.html</files>
  <action>
Add a "Production Sheet" button to the order detail view in preview.html.

1. In the renderOrderDetail() function, find the print actions section where "Print Invoice" and "Print Label" buttons are rendered.

2. Add a "Production Sheet" button alongside them:
```html
<button class="print-action-btn" data-action="production-sheet" data-order-id="${order.id}">
  Production Sheet
</button>
```

3. In the click handler for .print-action-btn elements (already exists), add a case for 'production-sheet':
```javascript
if (action === 'production-sheet') {
  // Open production sheet PDF in new tab for viewing/printing
  window.open('/api/orders/' + encodeURIComponent(oid) + '/production-sheet', '_blank');
  btn.disabled = false;
  btn.textContent = origText;
  return;
}
```

The production sheet opens in a new tab (like a PDF viewer) rather than triggering a print dialog directly, since the user likely wants to review it first.

4. Style the button consistently with existing print action buttons (same .print-action-btn class).
  </action>
  <verify>grep -n "production-sheet" scripts/pipeline/preview.html | head -10</verify>
  <done>Production Sheet button added to order detail view, opens PDF in new tab on click</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Production sheet PDF generation from order detail view</what-built>
  <how-to-verify>
    1. Run: npm run preview (starts server at localhost:3456)
    2. Navigate to Orders tab
    3. If no orders exist, click "Sync WIX Orders" to pull test data, OR create a manual order
    4. Click on any order to view order detail
    5. Locate the "Production Sheet" button alongside Invoice/Label buttons
    6. Click "Production Sheet" button
    7. Verify: PDF opens in new browser tab
    8. Verify PDF contains:
       - "PRODUCTION SHEET" header with order number
       - Customer/order summary section
       - Product sections with name, style number
       - Quantities table showing Color | Size | Qty
       - Clean, scannable layout
    9. If order has collection matching a customer with logos, verify logo placement indicator appears
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Production Sheet button visible in order detail view
- [ ] Button click opens PDF in new tab
- [ ] PDF contains all required sections (header, summary, products, quantities)
- [ ] Human verification approved
</verification>

<success_criteria>

- All tasks completed
- Production sheet accessible from dashboard order detail
- PDF layout is clean and production-ready
- Human verification passed
</success_criteria>

<output>
After completion, create `.planning/phases/38-production-sheets/38-02-SUMMARY.md`
</output>
