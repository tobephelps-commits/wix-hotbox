---
phase: 57-pi-e2e-test-automation
plan: 05
type: execute
wave: 2
depends_on: ["57-01"]
files_modified: [tests/ui/navigation.test.ts, tests/ui/products.test.ts, tests/ui/orders.test.ts]
autonomous: true
---

<objective>
Write UI E2E tests for sidebar navigation, product creation flow, and order management tabs.

Purpose: Validate the primary user workflows — navigating between tabs, creating products, and managing orders through the browser UI.
Output: Passing Playwright browser tests for navigation, products tab, and orders tab.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-pi-e2e-test-automation/57-01-SUMMARY.md
@ui/src/App.tsx
@ui/src/components/Sidebar.tsx
@ui/src/components/ContentArea.tsx
@ui/src/components/products/ProductsTab.tsx
@ui/src/components/orders/OrdersTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test sidebar navigation and products tab</name>
  <files>tests/ui/navigation.test.ts, tests/ui/products.test.ts</files>
  <action>
  IMPORTANT: Before writing tests, read the UI source files to understand the actual DOM structure, CSS classes, data-testid attributes (if any), and tab switching mechanism. The sidebar and content area implementation determines what selectors to use.

  Create tests/ui/navigation.test.ts:
  - Test page loads at baseURL and renders the app shell (sidebar + content area)
  - Test all 5 sidebar tabs are visible with correct labels
  - Test clicking each tab shows the corresponding content panel
  - Test the active tab has visual indication (active class or styling)
  - Test sidebar responsive behavior at mobile viewport (768px) — verify collapse behavior

  Create tests/ui/products.test.ts:
  - Test products tab shows style lookup input field
  - Test entering an invalid style number shows error feedback
  - Test the UI elements are present: style input, vendor selector (if visible), lookup button
  - Do NOT test actual vendor API lookups (no credentials) — test the UI shell and error states
  - Test any loading states or empty states render correctly
  - If there are template/preset UI elements, verify they render
  </action>
  <verify>npx playwright test tests/ui/navigation.test.ts tests/ui/products.test.ts</verify>
  <done>Navigation between all tabs works; products tab renders style lookup interface; error states display correctly</done>
</task>

<task type="auto">
  <name>Task 2: Test orders tab UI</name>
  <files>tests/ui/orders.test.ts</files>
  <action>
  IMPORTANT: Read ui/src/components/orders/OrdersTab.tsx, OrderList.tsx, OrderDetail.tsx, BulkToolbar.tsx, and PipelineView.tsx to understand the actual component structure and DOM before writing tests.

  Create tests/ui/orders.test.ts using seededServer fixture (so there are orders to display):
  - Test orders tab shows order list with seeded orders
  - Test order list displays order numbers, statuses, dates
  - Test clicking an order shows order detail view
  - Test order detail shows line items, status history, customer info
  - Test status transition buttons/dropdowns are present on order detail
  - Test pipeline view toggle (if exists) shows pipeline visualization
  - Test bulk toolbar appears when orders are selected (if checkbox selection exists)
  - Test filtering/search on orders list (if filter controls exist)
  - Handle the case where the UI fetches orders via API — the seeded server should respond with test data
  </action>
  <verify>npx playwright test tests/ui/orders.test.ts</verify>
  <done>Orders tab renders order list, detail view works, pipeline visualization displays, bulk tools accessible</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx playwright test --project=ui tests/ui/navigation.test.ts` passes
- [ ] `npx playwright test --project=ui tests/ui/products.test.ts` passes
- [ ] `npx playwright test --project=ui tests/ui/orders.test.ts` passes
</verification>

<success_criteria>
- All UI tests pass in Chromium
- Tab navigation works correctly
- Products tab renders style lookup interface
- Orders tab displays and interacts with order data
- Tests use proper selectors based on actual DOM structure
</success_criteria>

<output>
After completion, create `.planning/phases/57-pi-e2e-test-automation/57-05-SUMMARY.md`
</output>
