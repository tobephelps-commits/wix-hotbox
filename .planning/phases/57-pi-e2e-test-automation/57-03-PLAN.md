---
phase: 57-pi-e2e-test-automation
plan: 03
type: execute
wave: 2
depends_on: ["57-01"]
files_modified: [tests/api/orders.test.ts, tests/api/monitor.test.ts, tests/api/sync.test.ts]
autonomous: true
---

<objective>
Write API endpoint tests for order management, inventory monitoring, and stock sync.

Purpose: Validate the order lifecycle (CRUD, status transitions, sync) and inventory monitoring (tracked products, snapshots, alerts, sync daemon).
Output: Passing API tests for /orders, /monitor, and /sync endpoint groups.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-pi-e2e-test-automation/57-01-SUMMARY.md
@src/routes/orders.ts
@src/routes/monitor.ts
@src/routes/sync.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test order management endpoints</name>
  <files>tests/api/orders.test.ts</files>
  <action>
  Create tests/api/orders.test.ts using seededServer fixture (which pre-populates orders):
  - Test GET /api/orders returns 200 with array of orders
  - Test GET /api/orders/:id returns 200 for seeded order, 404 for non-existent
  - Test PATCH /api/orders/:id/status with valid transition returns 200
  - Test PATCH /api/orders/:id/status with invalid transition returns 400
  - Test GET /api/orders/summary returns 200 with order count/status breakdown
  - Test GET /api/orders/errors returns 200 with array
  - Test POST /api/orders/sync — should handle gracefully without WIX credentials (return error or empty result)
  - Test GET /api/orders/:id/invoice — for seeded order, should return PDF (content-type application/pdf) or appropriate error
  - Test GET /api/orders/:id/production-sheet — similar to invoice
  - Test POST /api/orders/bulk/status with array of order IDs
  - Test GET /api/orders/cart/preview returns 200
  - Do NOT test cart/fill (requires real vendor web credentials and Playwright browser)
  </action>
  <verify>npx playwright test tests/api/orders.test.ts</verify>
  <done>Order API tests pass; CRUD, status transitions, summaries, and PDF generation endpoints validated</done>
</task>

<task type="auto">
  <name>Task 2: Test inventory monitoring and sync endpoints</name>
  <files>tests/api/monitor.test.ts, tests/api/sync.test.ts</files>
  <action>
  Create tests/api/monitor.test.ts:
  - Test GET /api/monitor/config returns 200 with monitor configuration
  - Test POST /api/monitor/products adds a tracked product, returns 201
  - Test GET /api/monitor/products returns the added product
  - Test PATCH /api/monitor/products/:style updates product thresholds
  - Test DELETE /api/monitor/products/:style removes product
  - Test GET /api/monitor/alerts returns 200 with array
  - Test POST /api/monitor/poll — should handle gracefully without vendor credentials

  Create tests/api/sync.test.ts:
  - Test GET /api/sync/mappings returns 200 with array
  - Test POST /api/sync/mappings creates a mapping
  - Test DELETE /api/sync/mappings/:style removes mapping
  - Test GET /api/sync/health returns 200 with daemon status (should show not running in test)
  - Test POST /api/sync/start and POST /api/sync/stop for daemon control
  - Test POST /api/sync/audit — should work even without real WIX credentials (may return errors)
  </action>
  <verify>npx playwright test tests/api/monitor.test.ts tests/api/sync.test.ts</verify>
  <done>Monitor and sync API tests pass; tracked product CRUD, alert retrieval, daemon control, and mapping management validated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx playwright test --project=api tests/api/orders.test.ts` passes
- [ ] `npx playwright test --project=api tests/api/monitor.test.ts` passes
- [ ] `npx playwright test --project=api tests/api/sync.test.ts` passes
</verification>

<success_criteria>
- All API tests pass
- Order lifecycle (CRUD, status transitions) works correctly
- Monitor CRUD operations validated
- Sync daemon control and mappings work
- Tests handle missing external credentials gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/57-pi-e2e-test-automation/57-03-SUMMARY.md`
</output>
