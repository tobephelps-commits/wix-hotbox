---
phase: 57-pi-e2e-test-automation
plan: 06
type: execute
wave: 2
depends_on: ["57-01"]
files_modified: [tests/ui/inventory.test.ts, tests/ui/customers.test.ts, tests/ui/system.test.ts]
autonomous: true
---

<objective>
Write UI E2E tests for inventory monitoring, customer management, and system tabs.

Purpose: Validate the remaining UI tabs — inventory tracking display, customer CRUD forms, and system health dashboard.
Output: Passing Playwright browser tests for inventory, customers, and system tabs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-pi-e2e-test-automation/57-01-SUMMARY.md
@ui/src/components/Sidebar.tsx
@ui/src/components/ContentArea.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test inventory and customers tabs</name>
  <files>tests/ui/inventory.test.ts, tests/ui/customers.test.ts</files>
  <action>
  IMPORTANT: Before writing tests, read the actual UI component source files for these tabs to understand DOM structure. Check ui/src/components/ for inventory and customer components — they may be inline in ContentArea.tsx or in subdirectories.

  Create tests/ui/inventory.test.ts:
  - Test inventory tab renders (navigate via sidebar click)
  - Test tracked products list displays (may be empty in test env)
  - Test "add product" form/button is present
  - Test adding a tracked product via the UI form (if it has a form — fill in style number and submit)
  - Test alert list section renders
  - Test sync/poll controls are present
  - Test health status display (daemon status indicator)

  Create tests/ui/customers.test.ts using seededServer fixture:
  - Test customers tab renders with customer list
  - Test "add customer" button/form is present
  - Test creating a new customer via the UI form (fill name, markup, royalty rate)
  - Test customer appears in the list after creation
  - Test clicking a customer shows detail/edit view
  - Test editing customer markup and saving
  - Test deleting a customer
  </action>
  <verify>npx playwright test tests/ui/inventory.test.ts tests/ui/customers.test.ts</verify>
  <done>Inventory tab renders monitoring interface; customers tab supports full CRUD via UI</done>
</task>

<task type="auto">
  <name>Task 2: Test system tab and logo manager</name>
  <files>tests/ui/system.test.ts</files>
  <action>
  IMPORTANT: Read the UI source to understand what the system tab displays. Check ContentArea.tsx and any system-related components.

  Create tests/ui/system.test.ts:
  - Test system tab renders health/system information
  - Test CPU, memory, disk metrics are displayed (values from /api/system)
  - Test network information section shows LAN URLs
  - Test logo manager section renders (if logos are part of a tab)
  - If logo manager is in its own section/tab, test logo upload UI:
    - File input is present
    - Upload a test image
    - Logo appears in the logo list
    - Delete logo via UI
  - Test printer section (if printers are shown in system tab):
    - Discover button exists
    - Saved printers list renders
  </action>
  <verify>npx playwright test tests/ui/system.test.ts</verify>
  <done>System tab displays health metrics and network info; logo and printer management UI validated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx playwright test --project=ui tests/ui/inventory.test.ts` passes
- [ ] `npx playwright test --project=ui tests/ui/customers.test.ts` passes
- [ ] `npx playwright test --project=ui tests/ui/system.test.ts` passes
</verification>

<success_criteria>
- All UI tests pass in Chromium
- Inventory tab renders monitoring interface
- Customer CRUD works through UI
- System tab displays health metrics
- Logo management UI functional
</success_criteria>

<output>
After completion, create `.planning/phases/57-pi-e2e-test-automation/57-06-SUMMARY.md`
</output>
