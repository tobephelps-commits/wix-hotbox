---
phase: 57-pi-e2e-test-automation
plan: 04
type: execute
wave: 2
depends_on: ["57-01"]
files_modified: [tests/api/customers.test.ts, tests/api/logos.test.ts, tests/api/printing.test.ts]
autonomous: true
---

<objective>
Write API endpoint tests for customer management, logo system, and printing.

Purpose: Validate customer CRUD with pricing/royalty, logo upload/management, and printer discovery/print job endpoints.
Output: Passing API tests for /customers, /logos, and /printing endpoint groups.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-pi-e2e-test-automation/57-01-SUMMARY.md
@src/routes/customers.ts
@src/routes/logos.ts
@src/routes/printing.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test customer and logo management endpoints</name>
  <files>tests/api/customers.test.ts, tests/api/logos.test.ts</files>
  <action>
  Create tests/api/customers.test.ts:
  - Test POST /api/customers creates customer with name, markup, royaltyRate
  - Test GET /api/customers returns array including created customer
  - Test GET /api/customers/:id returns single customer
  - Test PATCH /api/customers/:id updates customer markup
  - Test DELETE /api/customers/:id removes customer
  - Test POST /api/customers/:id/pricing with sample product data returns calculated prices
  - Test GET /api/customers/:id/royalty returns royalty report (may be empty if no orders)
  - Test GET /api/customers/:id/royalty/pdf returns PDF content-type or appropriate error for no data

  Create tests/api/logos.test.ts:
  - Test GET /api/logos returns 200 with array (initially empty or with test logo)
  - Test POST /api/logos/upload with a small PNG buffer (create a 1x1 pixel PNG in test)
  - Test GET /api/logos lists the uploaded logo
  - Test GET /api/logos/file/:name serves the logo image
  - Test PUT /api/logos/:name updates logo metadata
  - Test DELETE /api/logos/:name removes logo
  - For the upload test, construct a minimal valid PNG buffer (8-byte header + IHDR + IDAT + IEND chunks) or use a tiny test fixture file
  </action>
  <verify>npx playwright test tests/api/customers.test.ts tests/api/logos.test.ts</verify>
  <done>Customer CRUD, pricing calculation, royalty endpoints, and logo CRUD all tested and passing</done>
</task>

<task type="auto">
  <name>Task 2: Test printing endpoints</name>
  <files>tests/api/printing.test.ts</files>
  <action>
  Create tests/api/printing.test.ts:
  - Test POST /api/printing/discover returns 200 with array (may be empty if no printers on network)
  - Test GET /api/printing/saved returns 200 with array (initially empty)
  - Test POST /api/printing/saved saves a printer config, returns 201
  - Test GET /api/printing/saved returns the saved printer
  - Test PUT /api/printing/default/:id sets default printer
  - Test DELETE /api/printing/saved/:id removes saved printer
  - Test POST /api/printing/print with invalid printer URI returns appropriate error
  - Test POST /api/printing/printers/test with unreachable URI returns error (not crash)
  - All printer tests should handle the case where no real printers exist — test the API contract, not actual printing
  </action>
  <verify>npx playwright test tests/api/printing.test.ts</verify>
  <done>Printing API contract validated — saved printer CRUD, discovery response shape, and error handling confirmed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx playwright test --project=api tests/api/customers.test.ts` passes
- [ ] `npx playwright test --project=api tests/api/logos.test.ts` passes
- [ ] `npx playwright test --project=api tests/api/printing.test.ts` passes
</verification>

<success_criteria>
- All API tests pass
- Customer lifecycle (CRUD, pricing, royalty) validated
- Logo upload/serve/delete cycle works
- Printing API contract tested (CRUD, discovery, error handling)
</success_criteria>

<output>
After completion, create `.planning/phases/57-pi-e2e-test-automation/57-04-SUMMARY.md`
</output>
