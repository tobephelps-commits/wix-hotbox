---
phase: 57-pi-e2e-test-automation
plan: 02
type: execute
wave: 2
depends_on: ["57-01"]
files_modified: [tests/api/health.test.ts, tests/api/system.test.ts, tests/api/vendors.test.ts, tests/api/pipeline.test.ts]
autonomous: true
---

<objective>
Write API endpoint tests for core system endpoints and the product pipeline.

Purpose: Validate health checks, system info, vendor registry, and the product creation pipeline — the most critical API surfaces.
Output: Passing API tests for /api/health, /api/system, /api/network, /vendors, and /pipeline endpoints.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-pi-e2e-test-automation/57-01-SUMMARY.md
@src/routes/index.ts
@src/routes/pipeline.ts
@src/routes/vendors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test core system endpoints — health, system, network, vendors</name>
  <files>tests/api/health.test.ts, tests/api/system.test.ts, tests/api/vendors.test.ts</files>
  <action>
  Create tests/api/health.test.ts:
  - Import { test, expect } from '../fixtures'
  - Test GET /api/health returns 200 with { status: 'ok' } or similar
  - Test response time is under 500ms (health check should be fast)

  Create tests/api/system.test.ts:
  - Test GET /api/system returns 200 with CPU, memory, disk info
  - Verify response has expected shape: { cpu, memory, disk, network } or similar
  - Test GET /api/network returns 200 with hostname and urls array

  Create tests/api/vendors.test.ts:
  - Test GET /api/vendors returns 200 with array of vendors
  - Verify vendor objects have id and name fields
  - Verify SanMar and S&S Activewear are in the list
  </action>
  <verify>npx playwright test tests/api/health.test.ts tests/api/system.test.ts tests/api/vendors.test.ts</verify>
  <done>All core API tests pass; health, system, network, and vendor endpoints validated</done>
</task>

<task type="auto">
  <name>Task 2: Test pipeline endpoints — preview, create, templates</name>
  <files>tests/api/pipeline.test.ts</files>
  <action>
  Create tests/api/pipeline.test.ts:
  - Test GET /api/pipeline/presets returns 200 with array of pricing presets
  - Test GET /api/pipeline/templates returns 200 with array (may be empty)
  - Test POST /api/pipeline/templates creates a template, then GET retrieves it, then DELETE removes it (CRUD cycle)
  - Test GET /api/pipeline/preview/:vendorId/:style with invalid vendor returns 400 or 404
  - Test GET /api/pipeline/preview/:vendorId/:style with invalid style returns appropriate error
  - Do NOT test with real vendor API calls (no SanMar/S&S credentials in test env) — test error handling for missing credentials or use mock data if the endpoint gracefully handles missing creds
  - Focus on response shapes, error codes, and CRUD operations that don't require external APIs
  </action>
  <verify>npx playwright test tests/api/pipeline.test.ts</verify>
  <done>Pipeline API tests pass; template CRUD validated, error handling for missing vendor credentials confirmed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx playwright test --project=api tests/api/health.test.ts` passes
- [ ] `npx playwright test --project=api tests/api/system.test.ts` passes
- [ ] `npx playwright test --project=api tests/api/vendors.test.ts` passes
- [ ] `npx playwright test --project=api tests/api/pipeline.test.ts` passes
</verification>

<success_criteria>
- All API tests pass
- Core endpoints return expected shapes
- Pipeline CRUD operations work
- Error cases handled properly
</success_criteria>

<output>
After completion, create `.planning/phases/57-pi-e2e-test-automation/57-02-SUMMARY.md`
</output>
